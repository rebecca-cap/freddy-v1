import { ThresholdTypes } from '@modules/CommandCenter/api/types.schema'

export const thresholdItemTypes: ThresholdTypes[] = [
  'Critical Below',
  'Critical Above',
  'Warning Below',
  'Warning Above',
]
