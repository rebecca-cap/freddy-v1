# Round 2 brief

React to Round 1 (see `context/round-1-review.md`) and resolve the three open questions.

## Focus

- **Zero-results state** — the headline fix. A blank table read as "broken"; design an explicit, reassuring empty state with the active filters echoed and ranked escapes. (R1 Open Q2)
- **Removable active-filter tokens** — a token row under the bar; each ✕ removes one filter. (R1 Open Q3)
- **Sticky bar on scroll** — pin the filter bar + token row to the top of the list region. (R1 Open Q1)
- Minor: group **Sort** with the filters; leave count + Clear all alone on the right.

## Kept from Round 1

- Status = chips, Product/Date = dropdowns. Additive (AND). Always-visible count + Clear all.

## Out of scope

- Saved views, per-column grid filters, multi-sort — unchanged, not this project.

## Outcome

All three R1 open questions resolved. This round is the direction to **promote to hi-fi**.
