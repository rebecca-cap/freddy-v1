# Quote Filter Bar — Compact Bar (variation v2b)

Generated by Project Hub on 2026-06-12T17:48:40.605Z.
This is variation `v2b` ("Compact Bar") of project "Quote Filter Bar", forked from version v2.
Branch: `variant/quote-filter-bar/compact-bar` (worktree of ~/freddy/proto-base)
Dev server URL when running: http://localhost:5186

**About the project:** Add a filter + sort bar to the QuoteBook quotes list so traders can quickly narrow by status, product, and date.

**Why this variation exists:** Single-row, brand-green treatment of the filter bar — chips + filters inline, no separate token row.

## Project context — it ALL applies here

This worktree is a FULL copy of the project: the entire codebase and its complete
git history up to the fork point (`git log` shows everything). Everything about
how "Quote Filter Bar" was designed and built — its architecture, conventions,
naming, and intent — applies to this variation unchanged. You are not starting
fresh; you are continuing an existing project on a client-specific branch.

Before doing anything substantial, load the parent project's context:

1. `~/freddy/projects/quote-filter-bar/CLAUDE.md` — the project's working agreement and paths
2. `~/freddy/projects/quote-filter-bar/README.md` — what the project is
3. If present in the parent folder: `project-context.md`, `PROJECT_NOTES.md`,
   `resources/REFERENCES.md` — design decisions, session history, references
4. `git log` in `./prototype/` — how the prototype actually evolved

The ONLY thing that makes this variation different from the mainline is what gets
committed on `variant/quote-filter-bar/compact-bar` after the fork — typically client-specific data/content.

## Scope — read this first

You are working on ONE variation of the prototype, NOT the project mainline.

- ALL edits happen in `./prototype/` — a git worktree on branch `variant/quote-filter-bar/compact-bar`.
- Do NOT touch the parent project's worktree (`~/freddy/projects/quote-filter-bar/prototype/`) or any other variation under `~/freddy/projects/quote-filter-bar/variants/`.
- This variation exists to diverge — typically client-specific data and content. Feature work that should reach every client belongs on the project mainline; flag it for the user instead of building it here.

## Paths

- `./prototype/`        — git worktree on `variant/quote-filter-bar/compact-bar` — edit here
- `./excalibrr-freddy`  — symlink to ~/freddy/excalibrr-freddy (do not edit)
- `~/freddy/projects/quote-filter-bar/` — parent project folder; briefs in `resources/`, wireframes in `design-system/lofi/` (read-only references)

## Run the prototype

```bash
cd prototype
VITE_PORT=5186 yarn start
```

Or use the Hub at http://localhost:5174 to start/stop it.

## Design references

UI work follows the vendored Excalibrr references in the Hub repo — component usage from `/Users/frankoverland/freddy/project-hub/resources/excalibrr-design-system/CLAUDE.md`, layout rules from `/Users/frankoverland/freddy/project-hub/resources/excalibrr-design-patterns/CLAUDE.md`.

## Working agreement for Claude Code

- When you finish a chunk, commit on branch `variant/quote-filter-bar/compact-bar`. Push only when explicitly asked.
- Leave the working tree clean between sessions — the Hub's "Pull from project" and "Promote to mainline" actions refuse to run on a dirty tree.
- Do NOT edit anything in `./excalibrr-freddy` (it's a symlink to a shared sibling).
- Build verification: the user runs `yarn build` themselves — skip it.
