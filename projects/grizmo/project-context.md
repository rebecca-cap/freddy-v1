# Grizmo — Project Context

Last updated: 2026-06-02 (Share-UI unification + Save/Edit tabs + Library card/list polish + sidebar chrome fix · committed through 2ef2dbd)

## Latest UI follow-ups (2026-06-02 pm, committed `2ef2dbd`)
- **Shared `GrizmoSharePicker.tsx/.css`** — extracted the scope-cards + scrollable avatar list out of the Share modal; BOTH `GrizmoShareModal` and `GrizmoSaveModal` now render the identical audience control. CSS scoped under `.grizmo-scope` (works in both modal wrappers); people list keeps `max-height:200px`. Save modal passes `includePrivate` (adds an "Only me" card, `LockOutlined`); Share modal omits it.
- Library card share icon swapped `ShareAltOutlined` → `UsergroupAddOutlined` to match the share modal head icon.
- **Save/Edit modal tabs** — `GrizmoSaveModal` split into **Details** (name/description/subject/meta/steps) and **Share** (the picker) via a local `tab` state + `.grizmo-save-modal__tabs` strip; resets to Details on open; footer Save/Cancel stays outside the tabs. Cut the modal's height (esp. edit-path).
- **Library card IA rebuild** (`GrizmoLibrary` `ItemCard` + css) — four zones: title + actions row → one tag-pills meta row (brand kind pill `Path · N steps`/`Prompt` + neutral scope + subject) → 2-line-clamped `<p>` description → footer (provenance left, CTA `margin-left:auto`). Edit/Share reveal on `:hover`/`:focus-within` (opacity, layout reserved so no reflow); **Star always visible**. 24px icon buttons, even 8px rhythm, `prefers-reduced-motion` guard. Dropped `__run-signal`/`__subject`/`__owner`/`__meta`.
- Library sections aligned to a shared **12px gutter** (search `8px 12px`, filter row `2px 12px 8px`, body `2px 12px 12px`) + even ~10px vertical rhythm.
- Library search field (`GrizmoViewSearch`) restyled to mirror the Ask field: pill radius, white surface, brand focus glow `0 0 0 3px rgba(113,93,170,0.18)`, 12px text — kept the lead search icon + × clear, no submit button, behavior unchanged. (Shared component but Activity dropped its search, so Library-only in practice.)
- **Sidebar-mode chrome fix** (`GrizmoPanel.css`) — panel stays full-height (`top:0`). The user cluster (`.control-panel-trigger`: avatar + name + Grizmo status tag + unseen badge) was hidden behind the full-height sidebar; now `body.grizmo-sidebar .control-panel-trigger { transform: translateX(calc(-1 * var(--grizmo-sidebar-width))) }` slides it left into the open band, aligned to the pushed grid's right edge. **Why transform, not toolbar padding:** an earlier `.site-toolbar { padding-right }` attempt got clipped by the `flex-1` menu-links region and pushed the cluster behind the panel. `--grizmo-sidebar-width` = `state.panelWidth` (set in `GrizmoActiveBodyClass`). Floating mode untouched.
- Verified visually via Frank's screenshots only (no Playwright run this session); dev :5181 200 throughout. `yarn build` is Frank's to run.

## Earlier UI follow-ups (2026-06-02, committed)
- `173bde3` — chat-list toolbar fill removed (transparent); "New chat" = brand-purple pill, white text, via the 4-class `.grizmo-scope .ant-btn.gravi-button.grizmo-inbox__new` override.
- `654b5d7` — chat inbox cards show a date + time stamp ("Jun 1 · 4:41 PM") from `thread.lastActivityAt` (`formatStamp` in GrizmoActivity.tsx), right-aligned footer.
- `f43d935` — circular chevron back button (`.grizmo-composer__back`, 35px, `LeftOutlined`) left of the Ask field; renders only when `state.view === 'chat'`; dispatches `setView('activity')` to return to the chat list.
- `94bd191` — **Library "+ Filters" add-filter pattern.** Favorites stays a standalone toggle; a "+ Filters" pill (`.grizmo-library__addfilter`, shrinks to compact `+` when active) opens an antd Dropdown (`overlay` + `items`) with Ownership/Type/Scope/Subject submenus (active value checked via `MenuValue`); picks render removable tokens (`.grizmo-library__token` + ×); "Clear all" at ≥2. Reuses existing filter state (no reducer changes); `OWNERSHIP_CHIPS` added. Menu styles avoid `--grizmo-*` (overlay portals outside `.grizmo-scope`; check color hardcoded `#715daa`). **Also fixed a chat-merge regression:** `showWelcome` now `!hasChats && !isLibrary` so the Library tab is reachable at zero chats (was showing the welcome).
- `6f3123b` — removed the `border-bottom` rule under the Library search (`GrizmoViewSearch` is Library-only now); Favorites chip set to `height:24px` to match the +Filters chip + tokens.

**Status: all committed on `project/grizmo`, NOTHING pushed.** Verify scripts/screenshots for these live under `/tmp/grismo-verify/` (cm-*, btn-*, back-*, filt-*, sizes-*).

## Status

**Chat-first merge + polish — COMPLETE & committed (2026-06-01).** Follow-up commits on top of the multi-threading build:
- `8bf0190` — `GRIZMO_THINK_MS` 10s → 20s (one constant drives every surface's transition).
- `83b7e54` — prototype timer bar made visible: was a faint light-gray strip at z-index 900 hidden under the QuoteBook "Quote Publisher" footer + nav sidebar. Now z-index 1000 (above app chrome, below antd modals which mount later), high-contrast bar, `pointer-events:none`.
- `f71e580` — brand **purple** (`#715DAA`) across the timer bar + cross-page chrome (name tag, indicator dot, drawer alert icons/cards) replacing amber `--theme-warning` / green `--theme-color-1`. Fixed product name **"Grismo" → "Grizmo"** (with a Z) everywhere user-facing (the requirements doc had spelled it "Grismo"). Chrome renders outside `.grizmo-scope` so purple is hardcoded.
- `2978a21` — **merged "Chat" + "Chats" into one chat-first flow.** Nav collapsed to `[Chats, Library]` (the standalone "Chat" tab removed; Chats is list-first and stays active inside a conversation, clicking it returns to the list — `GrizmoViewNav.tsx` `alsoActiveFor:['chat']`). **No seeded stream chat** — initial `view:'activity'`, `threads:[]`, `activeThreadId:null`, so a fresh session is genuinely zero-chats. **Welcome screen (`GrizmoEmptyState`) shows ONLY at zero chats** (`GrizmoPanel.tsx` `showWelcome = !hasChats`); a new/empty chat shows suggestion prompts as guidance only (`showGuidance`, `GrizmoChips standalone`) — no portrait/greeting. `ensureThreadForRow` null branch now continues the active chat (or creates a new general chat) instead of reusing a stream; `useGrizmoAsk` fallback label `'New chat'` + forces `setView('chat')` on ask. Terminology swept "thread" → "chat" in all user-facing copy ("All chats", "New chat", "No chats yet", drawer "Grizmo Chats"); `Thread`/`threadId` type identifiers unchanged. `STREAM_THREAD_ID`/`isStream` now vestigial. Verified end-to-end via Playwright.

## Status (multi-threading build)

**Multi-threading + cross-page alerts + global simulated timer — COMPLETE & committed (3d420ab, 2026-06-01).** Built from `~/Downloads/grismo-multithreading-requirements.md` via a dynamic Workflow swarm (foundation → 4 parallel surfaces → integration/verify). All 5 acceptance gates (A-THR/A-ROW/A-ALR/A-TMR/A-REG) PASS via real Playwright (real login, real /PricingEngine/QuoteBookEOD). Zero new Grizmo TS errors (the 1420 baseline + the page.tsx PublicationMode/permission errors are pre-existing at HEAD). Independently verified: disk diff, dev :5181 200, screenshots in `/tmp/grismo-verify/`.

- **Thread model (foundation).** `Thread { id, rowId, contextLabel, status: 'in-progress'|'ready'|'seen', messages, contexts, createdAt, lastActivityAt, readyAt, lastQuestion, answerPreview, isStream }` in `types.ts`. State gains `threads[]`, `activeThreadId`, `simNow`. **Mirror model:** `state.messages`/`state.contexts` are a LIVE MIRROR of the active thread — all 47 pre-existing reducer actions mutate them unchanged, and the outer `grizmoReducer` syncs the arrays back into `threads.find(activeThreadId)` after any mutating action, so existing components needed ZERO changes. New actions: `createThread`, `setActiveThread` (snapshots current, loads target slices, marks 'seen'), `markThreadSeen`, `startThreadThinking(id, readyAt)`, `tickTimer(nowMs)`, `ensureThreadForRow(rowId, label)`. Seed stream thread (`STREAM_THREAD_ID`, isStream, active, 'seen'). Derived: `unseenCount`, `grizmoActivityState`.
- **Timer engine.** `useGrizmoTimer` runs ONE 250ms `setInterval` inside the provider dispatching `tickTimer(epochMs)`; flips in-progress→ready when `simNow >= readyAt` (clears readyAt). `GRIZMO_THINK_MS = 10000`. Status transitions are driven ONLY by the timer, never by `askGrizmo` resolving (panel thinking-steps are now cosmetic). `useGrizmoAsk` calls `ensureThreadForRow` then `startThreadThinking(id, simNow + 10000)`.
- **CRITICAL hoist fix (caught by verify agent).** Foundation first wrapped `<GrizmoProvider>` around only `<Outlet/>` inside `NavigationContextProvider` — this (a) rendered the cross-page chrome OUTSIDE the provider → every non-QuoteBook page crashed with "useGrizmo must be used inside GrizmoProvider", and (b) remounted the provider on SPA nav → reset threads + broke the continuous-timer invariant. **Fix:** hoisted `<GrizmoProvider>` ABOVE the router in `src/modules/_Main/index.jsx` (wrapping `<RouterProvider>` in GateKeeper); `<GrizmoGlobalSurfaces/>` + `<GrizmoToastHost/>` mount there too. `AuthenticatedRoute` is now a plain consumer. Dispatch identity is stable across navigation.
- **Thread inbox (THR-3..7).** Activity tab repurposed (`GrizmoActivity.tsx`) into a stacked thread list (incl. stream); truthful status badges (in-progress=animated dots, ready=brand dot+accent rail, seen=muted); text-message-style slide-in (inbox view→chat view via `setView`, NO persistent side list — sidebar/docked width untouched); `+ New thread`; `GrizmoViewNav` Chats tab is the show-chats entry point with an unseen count pill.
- **Per-row indicator (ROW-1..3).** New pinned-left gutter column (`GrizmoRowReadyCell.tsx`) renders three dots that light (brand, pulsing) when the row's thread is ready, quiet shimmer while in-progress, absent when seen/no-thread/group-row. Click → `setActiveThread` + open panel → slides into that row's thread. Row keyed by `String(QuoteConfigurationMappingId)`. Refreshes via existing `refreshCells({force:true})` effect extended with threads/simNow deps. Overrides `pointer-events:none` so the indicator stays clickable under `.grizmo-active`.
- **Cross-page chrome (ALR-1..5).** `GrizmoNameTag.tsx` (tag left of name: "Grismo is thinking"/"Grismo is ready"/idle) + unseen dot in `UserSummary.tsx`; `GrizmoAlertCards.tsx` replaces the "Recent Alerts" `NothingMessage` in `ControlPanel.jsx` with thread cards mirroring `.alert-card`. Chrome renders OUTSIDE `.grizmo-scope` → uses `:root` theme tokens only (`--theme-color-1` for brand-ready per css-gotchas memo, NOT `--theme-color-2`). Card click → `setActiveThread` + navigate to QuoteBookEOD.
- **Bottom timer bar (TMR-2).** `GrizmoGlobalSurfaces.tsx` body-portals a fixed bottom bar (z 900, pointer-events:none), utilitarian scaffolding look (dashed top border, monospace, neutral grays + amber, literal "PROTOTYPE TIMER (simulated latency — not part of Grismo)"), soonest-ready countdown `ceil((readyAt - simNow)/1000)`. Reads simNow only — no own timer; continuous across nav automatically.
- **Open follow-ups (non-blocking):** (1) timer bar also renders on /login (provider above auth gate) — could gate to authenticated branch; (2) unused `state.search.activity` slice now that inbox dropped its search; (3) GraviButton `type='primary'` on New-thread + ready-tag color want a cross-theme eyeball; (4) optional matching chat-enter transition for slide-in symmetry.

## Prior status

**Phase 7 complete.** Header actions (Undo · Clear · Minimize · Close), close-confirmation modal, single-toast renderer, and the "wipe backups on next ask" audit all ship. Verified end-to-end via Playwright (`$CLAUDE_JOB_DIR/verify-phase7.mjs`): 11 stages green — empty header (Clear disabled, no Undo) → seed conversation (Clear enables) → Clear (Undo appears, "Conversation cleared" toast) → Undo restores (messages back, "Restored" toast, Undo hidden) → Minimize (panel unmounts, state preserved) → Close (confirmation modal opens) → Cancel ("Keep going" closes modal, panel stays) → Discard (panel closes, backups populated, Undo recovers on re-open) → new ask after Discard wipes backups (Undo disappears) → view-switching dance preserves backups → empty-backup Undo logs no error. Screenshots `$CLAUDE_JOB_DIR/p7-01..p7-08.png`. TS clean (zero new Grizmo errors), dev :5181 still 200.
- **7.1 Header actions** — Panel header gains a `.grizmo-panel__controls` cluster (right of the view-nav) with four icon-only `GraviButton`s wrapped in antd `Tooltip`s: Undo (UndoOutlined, conditional render when `messagesBackup` OR `contextsBackup` is non-null), Clear (DeleteOutlined, disabled when `messages.length === 0 && contexts.length === 0`), Minimize (MinusOutlined → `setOpen(false)` keeping state), Close (CloseOutlined → `openCloseConfirm()` — always shows modal even when empty, per Round-4 fix). Old single-`.grizmo-panel__close` selector replaced with `.grizmo-panel__ctl` shared baseline; Undo gets a warm-amber `grizmo-undo-glow` 2.4s pulse so it's discoverable.
- **7.2 Close-confirm modal** — `GrizmoCloseConfirm.tsx/.css`. Portals to `document.body` (so the backdrop scrim covers the panel) with `.grizmo-scope` on the backdrop so tokens cascade. Layout mirrors wireframe verbatim: red `ExclamationCircleFilled` icon disc → "End this session?" title → contextual description (counts visible answers + contexts; falls back to terse copy when both empty) → two stacked `GraviButton`s ("Minimize instead" brand-purple, "Close & discard" danger-red) → "Keep going" cancel link. Discard fires a single `discardSession` reducer action (stash → wipe → setOpen(false) → close modal — one re-render instead of four). Escape key (capture phase) cancels; backdrop click cancels; focus lands on Minimize via rAF.
- **7.3 Undo button** — Part of 7.1's control cluster, but logic-wise the standout: hidden entirely when both backups are null; click dispatches `restoreLast` (which restores messages + contexts AND nulls both backups + `lastClearedAt` in one action) then fires a `Restored` toast; if pressed while backups happen to be empty (programmatic / keyboard race), surfaces "Nothing to undo" warn toast. Cell highlights re-apply automatically because `getCellState` reads from `state.contexts` and `restoreLast` re-populates contexts in the same dispatch.
- **7.4 Toast system** — `GrizmoToastHost.tsx/.css`. Portals to `document.body` (so it sits above panel + modal — `z-index: 1100`). Renders only `state.toasts[toasts.length - 1]` (single-toast policy), keyed by `toast.id` so the auto-dismiss `setTimeout(2500ms)` resets cleanly when a new toast replaces the old. **Reducer change:** `addToast` now REPLACES the toasts array with a single-element list (not append) — the wireframe's `showToast` semantics. Without this, when toast A auto-dismissed the previous toast B resurfaced with a fresh timer (caught in verify run). Visual: bottom-center pill, `--grizmo-text` background, white text, soft shadow, fades in via `grizmo-toast-in` keyframe. `kind` variants: `success` (green), `warn` (amber), `error` (red), default (dark).
- **7.5 Backup/restore audit** — `useGrizmoAsk` now dispatches `clearBackups` at the START of every `ask()` when either backup is non-null — implements the Round-4 fix where "starting a new ask after clear silently wiped the backup, so undo could never recover." Path runner (`useGrizmoRunPath`) calls `ask()` per step, so it inherits the same rule (each path step counts as a new ask). View switching (`setView`) does NOT touch backups — reducer only mutates `view`. `discardSession` and `clearConversation` both populate backups + zero out live state. Undo (`restoreLast`) restores + nulls both backups + clears `lastClearedAt` atomically.
- **State additions** — `GrizmoState` gains `closeConfirmOpen: boolean`. New reducer actions: `openCloseConfirm`, `closeCloseConfirm`, `discardSession`, `clearBackups`. Bound action creators added. `addToast` semantics changed (replace, not append).
- **Mount changes** — `page.tsx` adds `<GrizmoToastHost />` as the last child of `<GrizmoProvider>` (alongside Panel, Bubble, Shortcuts, ActiveBodyClass).
- **Phase 7.1b mini-bubble (follow-up)** — Frank flagged "minimize visually looks like close, I don't see a difference." Plan didn't include the wireframe's mini-bubble; added it as a follow-up. New `GrizmoMini.tsx/.css` (brand-purple pill, white "G" disc, "Grizmo" label). **Anchoring (post-feedback iteration):** mini pill positions ABOVE the `#grizmo-trigger` G button with a 10px gap rather than the wireframe's top-right viewport corner — Frank wanted it close to where the user expects Grizmo. Measurement strategy mirrors `GrizmoIntroBubble` (60-frame rAF poll after mount + ResizeObserver for steady state + window resize/scroll listeners). JS sets inline `left` (centered on trigger via `transform: translateX(-50%)`) and `bottom` (window.innerHeight - rect.top + 10); CSS owns the centering transform and the slide-in/shimmer animations. Adds a subtle diagonal white-sheen `::after` shimmer (3.4s ease-in-out) so the minimized state grabs the eye. New `isMinimized: boolean` state flag, new `minimize` reducer action (`open=false, isMinimized=true` in one dispatch). `setOpen(true)` and `toggleOpen` auto-clear `isMinimized` so the mini-bubble vanishes the moment the panel opens by any path. `discardSession` explicitly sets `isMinimized=false`. Header Minimize button and modal "Minimize instead" both dispatch `minimize()`. Verified via `verify-mini.mjs`: 5 stages green. Screenshot `$CLAUDE_JOB_DIR/p7m-01-minimized.png`.

- **Header restructure (follow-up #2)** — Frank flagged "Grizmo header title disappeared and was replaced by tabs. Put it back and put the tabs right underneath." Phase 5.1 had collapsed the panel's "Grizmo" `Texto` title in favor of the 3-tab nav. Restructured `GrizmoPanel.tsx` header into TWO rows: row 1 = `[G] Grizmo` title (22px purple disc + label) + control cluster (Undo / Clear / Min / Close), row 2 = `<GrizmoViewNav />`. CSS in `GrizmoPanel.css`: `.grizmo-panel__header` becomes `flex-direction: column`; new `.grizmo-panel__title-row` (padding 8px 8px 6px 12px, bottom-border for divider), new `.grizmo-panel__g` (22px purple circle, white bold G). `GrizmoViewNav.css`: dropped `flex: 1` and the `margin-bottom: -10px` negative-margin trick (used to align the underline with the panel border) — instead the nav gains its own `border-bottom: 0.5px` and `padding: 0 4px`. Phase 7 verify still passes; visual confirmed via `header-rework.png`.

- **Auto-fit height + height-only resize (follow-up #4)** — Frank: panel should fit content by default (was fixed 540px with internal scroll), grow with content up to 80vh, then scroll; height adjustable via a corner grip but width never. New `panelHeight: number | null` state (`null` = auto-fit), `setPanelHeight` action, reset to null on `discardSession`, persists across minimize. CSS: `.grizmo-panel-frame` height auto (was fixed); `.grizmo-panel` drops `height:100%`, gains `max-height: min(80vh, calc(100vh - 66px))` + content sizing; `.grizmo-panel__body` `flex: 1` → `flex: 1 1 auto` (basis auto) so it shrink-wraps short content and only scrolls when the panel is capped/pinned. Resize: `.grizmo-panel__resize` grip at bottom-right (diagonal lines via `repeating-linear-gradient` clipped to a triangle, `cursor: ns-resize`), pointer-capture drag updates a local `resizeHeight` (committed to `state.panelHeight` on pointerup), clamped [220px, 80vh]. Width is never touched. Footer padding bumped to `10px 26px 16px 12px` to clear the Ask button from the grip. Verified via `verify-resize.mjs`: empty 600px (fits, no scroll) → 1 short answer 427px (shrinks to fit) → many answers cap exactly at 720px (80vh) → grip drag sets height with width unchanged → persists across minimize → discard resets to null. Note: empty state is ~600px tall now because the 150px portrait + 2-row header + chips genuinely need it (no longer clipped to 540 with scroll).

- **Animated conic-gradient shimmer border (follow-up #3)** — Frank requested "purple gradient border, animates, light shimmering around it like rotating — like a thinking machine." Wrapped the `<aside class='grizmo-panel'>` in a new `<div class='grizmo-panel-frame'>` so the gradient can extend past the panel's `overflow: hidden`. The frame owns `position: fixed; top: 50px; right: 16px; width/height` (moved from the panel). New `<div class='grizmo-panel-frame__shimmer' aria-hidden />` sits absolute `inset: -3px` inside the frame at `z-index: 0`. The shimmer is a conic gradient that rotates via `@property --grizmo-shimmer-angle` (registered angle custom property — required for browsers to interpolate `from var(...)` in `conic-gradient`, otherwise the gradient snaps each tick instead of sweeping). Animation: `grizmo-shimmer-spin` 6s linear infinite. Gradient stops cycle through `--grizmo-brand` → `--grizmo-brand-soft-2` → `#fff` → `--grizmo-brand-soft` and back, producing two bright "light hotspots" that orbit the panel every 6s. **Iteration history:** v1 used `brand-hover` (#5C4A91) at endpoints — too dark; v2 lightened to brand → brand-soft-2 → soft → white. v1 also had a `drop-shadow(0 0 12px rgba(113, 93, 170, 0.6))` purple haze — Frank flagged "shadow is intense"; dropped it entirely. Final: `filter: blur(1.5px)` only (softens conic banding), panel's own neutral `--grizmo-shadow-lg` handles elevation. Respects `prefers-reduced-motion: reduce`. Panel `.grizmo-panel` switches from `position: fixed` to `position: relative; width: 100%; height: 100%`, keeps its own white background + border-radius + `overflow: hidden` + `z-index: 1` so the inner content stays above the shimmer ring. `.grizmo-panel-frame` is `pointer-events: none` with `> * { pointer-events: auto }` so clicks fall through the bare frame to children only.

**Phase 6 complete.** Save-as-path candidate prompt, modal w/ inline editor, sequential path runner, and aggregate-vs-row detector all ship. Verified end-to-end via Playwright (`$CLAUDE_JOB_DIR/verify-phase6.mjs`): ask first agg → no candidate (need 2) → ask second agg → candidate flag flips → inline prompt renders below latest bubble → "Save as path" opens modal → name validation toggles Save button → editor expands inline → drop step / reorder works → Save persists into `state.library.paths` (3 paths total, scope='agg', new path with 1 kept step) → switch to library → click Run on the new path → view auto-flips to chat, `isPathRunning` true → 1 user + 1 grizmo bubble streamed → runner resets → seed row-scoped path Run without row chip → warn toast surfaces, isPathRunning stays false.
- **6.1 Candidate prompt** — `GrizmoPathCandidate.tsx/.css`. Inline amber-tinted prompt with `PlayCircleOutlined` icon, "Save these N questions on (this row|the quote book) as a path?" lede + 2-line preview of step labels, "Save as path" (warn-tinted primary) + dismiss × button. Mounts directly under the latest grizmo bubble in `GrizmoMessageList` (new `.grizmo-message-list__item` wrapper groups bubble + prompt). Looks up the live candidate from `detectPathCandidate(messages)` so the modal always opens with the freshest set of steps even after follow-up answers.
- **6.2 Save-path modal** — `GrizmoPathModal.tsx/.css`. antd `Modal` with `visible` prop (antd 4 — see Phase 4 gotcha; same applies to Modal). Wrap class `grizmo-scope grizmo-path-modal-wrap` lets Grizmo tokens flow into antd's body. Layout: brand-tinted head with title + close × → body with Name `Input`, Description `Input.TextArea`, scope badge + kept-step meta, Steps inner block → footer with Cancel (ghost) + Save (id `pathSaveBtn`). Save disabled when name empty OR all steps dropped. Save persists into `state.library.paths`, marks each source grizmo message `usedInPath: true` (so detector shrinks for second-path saves, per wireframe), closes modal, flips library inner tab to `paths`, fires success toast.
- **6.3 Path editor** — Same modal, inline. "Edit steps" link toggles `pathModal.editorOpen`. When open, each step row swaps the label text for an `Input` + reveals three icon-only buttons: `ArrowUpOutlined` (move up — disabled at index 0), `ArrowDownOutlined` (move down — disabled at last), `CloseOutlined` (drop — flips to `UndoOutlined` for restore). Dropped steps render with `line-through` + opacity 0.45 + `–` in the number circle. All edits go through reducer actions (`updatePathStep`, `reorderPathStep`, `updatePathModal`) — modal stays a pure view + dispatcher.
- **6.4 Path runner** — `state/useGrizmoRunPath.ts`. Single async function: row-scope guard (warn toast + early return if no row/cell chip), `setView('chat')`, `setPathRunning(true)`, sequential `await ask({ promptId, label })` per step, `setPathRunning(false)` in `finally`. Library `Run` button disables + shows "Running…" label while `state.isPathRunning`. Library "Open prompt" wired too: `setView('chat')` then `ask({ freeText: prompt.body, label: prompt.label })`. Activity "Re-run" wired: same pattern. Activity "Save as path" seeds a single-step path modal from the activity entry (scope `agg`).
- **6.5 Detector** — `state/detectPathCandidate.ts`. Pure function over `GrizmoMessage[]`. Latest grizmo answer is aggregate when `mode === 'aggregate' | 'multi-row' | undefined && !rowId`. Aggregate candidate covers all aggregate-shaped prior answers; row candidate covers same-`rowId` prior answers. Skips messages already marked `usedInPath`. Collapses consecutive identical `promptId`s. Returns null when fewer than 2 unique steps remain. `useGrizmoAsk` synthesizes post-update messages locally (state is stale right after dispatch) and runs the detector after each answer; when triggered, flips `candidate: true` + `candidateScope` on the latest grizmo message via `updateMessage`.
- **State additions** — `GrizmoMessage` gains `promptId`, `question`, `candidate`, `candidateScope`, `usedInPath`. `PathStep` gains `promptId`, `mode`. New top-level `pathModal: PathModalState | null` (sourceMessageId, scope, name, description, steps with `dropped` flag, editorOpen) + `isPathRunning: boolean`. New reducer actions: `dismissCandidate`, `openPathModal`, `closePathModal`, `updatePathModal`, `updatePathStep`, `reorderPathStep`, `savePath`, `setPathRunning`. `GrizmoAnswer` gains optional `mode: 'agg' | 'row'`; all four `agg-*` scenarios stamped `agg`, `summarize-30-days` + `compare-three-quotes` stamped `row`, fallback unstamped.
- TS clean (zero new Grizmo errors), dev :5181 HTTP 200, screenshots `$CLAUDE_JOB_DIR/p6-01..p6-08.png`.

**Phase 5 complete.** All three views (Chat / Library / Activity) ship in the same panel with shared chrome, plus search + bookmark filtering with per-view state preservation. Verified via Playwright: 3 nav tabs render → click each → state.view flips → Library shows Paths|Prompts inner tabs with seed data (2+2) → search "margin" filters → × clear resets → bookmark toggle flips state → bookmarked-only filter narrows list → Activity shows 3 entries reverse-chrono with "3 min/22 min/1 h ago" timestamps → activity search + bookmark independent of library → switching across all 3 views preserves searches per-view ("triage" in library, "Pilot" in activity, DOM inputs re-sync from store) → keyboard Tab/Space/Enter on view-nav tabs activates the view.
- **5.1 View nav** — `GrizmoViewNav.tsx/.css` mounts in panel header (replaced the old "Grizmo" title + close-only chrome). Three real `<button role='tab'>`s with antd icons (MessageOutlined, BookOutlined, HistoryOutlined). Active tab gets brand-purple ink + 2px brand underline + semibold weight. Composer footer + context chips strip render only when `state.view === 'chat'`.
- **5.2 Library** — `GrizmoLibrary.tsx/.css`. Top: shared `GrizmoViewSearch view='library'`. Below: inner "Paths | Prompts" tab strip (each tab has a count pill — 2/2 seeded). Body: filtered card list. Each path card = title + description + bookmark icon top-right + meta "▶ N steps · Row/Aggregate" + `GraviButton buttonText='Run'` (console.log stub). Each prompt card = title + description + bookmark + `GraviButton buttonText='Open'` (stub). Two empty states (no-items-at-all per tab, no-matches with "Clear filters" link).
- **5.3 Activity** — `GrizmoActivity.tsx/.css`. Top: shared `GrizmoViewSearch view='activity'`. Below: reverse-chrono list. Each row: relative timestamp (xs, subtle) + bold title + ellipsised description + 3 icon-only action buttons (bookmark, ReloadOutlined re-run, SaveOutlined save-as-path — last two are console.log stubs). `formatRelative(ms)` emits "just now / 3 min ago / 1 h ago / 2d ago". Two empty states (no-activity-yet, no-matches).
- **5.4 Search** — Shared `GrizmoViewSearch.tsx/.css`. Local-state input → 150ms debounce → `setSearch(view, query)`. × button bypasses debounce. Component re-syncs local from upstream when store changes externally (so switching tabs restores DOM input value). One component serves library + activity, parameterized by `view` prop.
- **5.5 Bookmarks** — Custom inline-SVG `GrizmoBookmarkIcon.tsx` (ribbon shape, 14px default, `currentColor`). Filled vs outlined via `filled` prop. antd has no first-class bookmark icon and the plan mandates "not a star". Each card / activity row has a bookmark `<button>` dispatching the appropriate toggle reducer action; bookmarked-only filter in the shared search bar is itself a bookmark-icon button that fills brand-purple when active.
- **5.6 State preservation** — Verified: per-view searches under `state.search.{library,activity}` and per-view bookmarked filter under `state.bookmarkedOnly.{library,activity}`; switching across views never mutates messages/contexts/library/activity. Tab buttons are real `<button role='tab'>`s — keyboard Tab/Space/Enter all work.
- **State additions** — `bookmarked: boolean` on `SavedPath`; `description` added to `SavedPrompt`; `libraryInnerTab: 'paths'|'prompts'`; `search: { library, activity }`; `bookmarkedOnly: { library, activity }`. Six new reducer actions: `setLibraryInnerTab`, `setSearch`, `setBookmarkedOnly`, `toggleLibraryPathBookmark`, `toggleLibraryPromptBookmark`, `toggleActivityBookmark`. Seeded 2 paths + 2 prompts + 3 activity entries with staggered timestamps.
- TS clean (zero new Grizmo errors), dev :5181 HTTP 200, screenshots in `$CLAUDE_JOB_DIR`: p5-01..p5-10.

**Phase 4 complete.** Cell color states, column-group tints, toolbar legend, "How cell colors work" popover, context chips bar with +Add, and chips ↔ grid wiring are all live. Verified via Playwright: a seeded row chip lights 15 data cells scope-green + 2 identity cells gray on the targeted row in real time; an explicit cell chip lights one specific cell and applies the brand-purple "is-explicit" badge.
- **4.1 Cell color states** — `styles/grizmo-grid.css` defines `.grizmo-active .ag-cell.grizmo-cell--scope` (green tint + inset 1.5px outline + accent text) and `.grizmo-active .ag-cell.grizmo-cell--identity` (5%-black overlay + slightly darker ink). Both gated by `body.grizmo-active`, toggled by `GrizmoActiveBodyClass`. Transitions: `background-color 120ms ease-out, color 120ms`.
- **4.2 Column groups & legend** — Cells inherit `grizmo-col--<group>` via the wiring walker; CSS gates tints behind `.grizmo-active` and beats AG Grid baseline. `GrizmoColumnLegend.tsx/.css` ships a 6-row swatch strip ("Whole row · Cost · Proposed · Margin · Δ vs prior · Flags") mounted at the front of the QuoteBook toolbar; renders null when `!state.open` so the grid stays clean.
- **4.3 Tour popover** — `GrizmoTourTrigger.tsx/.css` ships a pill button + antd Popover. Auto-opens 300ms after panel-open ONCE per session unless `state.tourDismissed`. Content: portrait + "Cell colors scope Grizmo" header + identity/scope rows (swatch + name + 1-line description). Outside click → onVisibleChange(false) → dispatches `dismissTour`.
- **4.4 Context chips bar** — `GrizmoContextChips.tsx/.css` mounted in `GrizmoPanel` between header and body. Renders `state.contexts` as pills with × buttons, empty-state copy ("No context — Grizmo will answer about the whole view."), and a "+ Add" pill (brand-active when `addContextMode`). Cell chips get an `is-explicit` brand-outline shadow when `chip.explicit === true`. "Clear all" appears when `contexts.length >= 2`.
- **4.5 +Add context mode** — New `addContextMode` flag in store + `toggleAddContextMode` reducer action. `body.grizmo-add-context-mode` class adds `cursor: copy` to grid cells + dashed brand outline on hover. Escape key in `GrizmoShortcuts` exits the mode (preventDefault only when active). +Add click in chips bar toggles the mode; next cell click appends `{ kind: 'cell', columnId: field, explicit: true }` chip then exits mode. Same cell clicked twice in add mode → toggles off.
- **4.6 Chips ↔ grid wiring** — `grid/grizmoScope.ts` exports `getCellState({rowId, field}, contexts)` with the 4 rules: explicit cell chip → scope; selection chip matching scope group → scope; row chip + identity column → identity; row chip + data column → scope. `grid/withGrizmoCellWiring.ts` walks the column-defs tree, composing each leaf's `cellClass`/`headerClass`/`onCellClicked` with Grizmo-aware augmenters that read from live refs (contexts + open). `useGrizmoGridWiring(colDefs, gridApiRef)` calls `gridApi.refreshCells({force:true})` whenever contexts or open change so AG Grid re-evaluates the cellClass. Wired into `EndOfDay.tsx` via a one-line cast.
- **Replace-vs-append rule** — Default cell click replaces all non-explicit chips with one new chip (cell chip for data columns, row chip for identity columns). If any chip in state is `explicit === true`, every subsequent click appends instead. Clicking a chip's own target toggles it off.
- **Click intercept** — `composeOnCellClicked` skips the host app's existing `onCellClicked` (drawer-opening, etc.) when `state.open === true`. Closed panel preserves existing QuoteBook behavior + first-click can still open Grizmo and seed a chip.
- **Tokens** — Added at `:root` (not `.grizmo-scope`) since AG Grid cells render outside any Grizmo container: `--grizmo-cell-scope-bg/text/outline`, `--grizmo-cell-identity-bg/text`, `--grizmo-col-<group>-bg/sw` for each of 5 scope groups.
- TS clean (zero new Grizmo errors), dev :5181 HTTP 200, Playwright runs capture chip rendering + cell highlights + add-mode + tour popover.

**Phase 3 complete.** Full conversation surface wired end-to-end. Chip click OR composer Enter both push a user bubble + placeholder Grizmo bubble, stream thinking steps in real time via `appendThinking` reducer action, then fill in body + sources + confidence when `askGrizmo` resolves. Verified via Playwright: chip flow (`agg-flagged` → 4 steps → Medium answer w/ 3 sources) AND free-text flow ("Quick check…" → `_fallback` → 3 steps → Low answer w/ 1 source).
- **3.1 Message list** — `GrizmoMessageList.tsx/.css`. Scrollable column, smooth scroll-to-bottom on every `state.messages` change (catches progressive step appends + final answer growth). Lives inside `.grizmo-panel__body.has-messages` (panel strips its own padding so the list owns it).
- **3.2 User bubble** — `.grizmo-msg--user` in `GrizmoBubble.tsx/.css`. Right-aligned, `--grizmo-info-bg` tint, `--grizmo-info` text, `max-width: 88%`, `white-space: pre-wrap`, `overflow-wrap: anywhere`.
- **3.3 Grizmo bubble shell** — `.grizmo-msg--grizmo`. Small 22px brand "G" avatar (raw inline circle — Excalibrr has no avatar primitive). Card with three slots: `slot--thinking`, `slot--body`, `slot--footer`. Each slot renders nothing when its data is empty.
- **3.4 Composer** — `GrizmoComposer.tsx/.css`. antd `Input.TextArea` with `autoSize={{ minRows: 1, maxRows: 5 }}`, Excalibrr `GraviButton` (4-class override `.gravi-button.grizmo-composer__send` to beat Excalibrr's `.gravi-button.filled.default { background }`). Enter submits, Shift+Enter newlines, focus returns to textarea via rAF after submit. Send disabled when empty OR busy.
- **3.5 Thinking steps** — `GrizmoThinkingSteps.tsx/.css`. Pure component: takes `Array<{id,label,status: 'pending'|'active'|'done'}>`, hides pending, animates active (brand pulse + gradient-clip shimmer copied verbatim from wireframe lines 407–419), checks for done. Auto-collapses after every step is done; user-toggle wins thereafter. Status computation lives in `GrizmoBubble` (`computeDisplaySteps`) so the steps component stays a pure view.
- **3.6 Answer body** — `GrizmoAnswerBody.tsx/.css`. Renders string (single paragraph) or `AnswerBlock[]` of `{ kind: 'p' | 'ul' | 'table', ... }`. Table has compact density, sticky-less header tint, 0.5px borders matching the wireframe's `table.grid`. All current scenarios still use strings; structured shape is opt-in for future compares.
- **3.7 Transparency footer** — `GrizmoSourcesFooter.tsx/.css`. Sources pills (real `<button>`s — keyboard accessible, click is `console.log` no-op for now) + confidence dot. Color map: High = `--grizmo-accent` (green), Medium = `--grizmo-warn` (amber), Low = `--grizmo-danger` (red). Renders nothing when both inputs are empty.
- **3.8 Wiring** — `state/useGrizmoAsk.ts` is the single ask() function: pushes user msg, pushes placeholder grizmo msg with empty thinking/body, streams steps via `appendThinking`, fills in answer via `updateMessage` with `thinkingComplete: true`. `GrizmoChips` and `GrizmoComposer` both consume `useGrizmoAsk()`. `GrizmoPanel` swaps empty state → message list when `state.messages.length > 0`; chips disappear automatically because they live inside `GrizmoEmptyState`.
- **State additions** — `updateMessage(id, partial)` and `appendThinking(id, step)` reducer actions; `GrizmoMessage.body` widened to `string | AnswerBlock[]`; new `thinkingComplete?: boolean` flag drives step-status computation in the bubble.
- **TS clean** (zero new Grizmo errors), dev :5181 HTTP 200, Playwright run captures correct bubbles + collapse + free-text path.

**Phase 2 complete.** Empty state (portrait + greeting + 4 closed-prompt chips) renders inside the panel body when `messages.length === 0 && contexts.length === 0`:
- **2.1 Portrait** — `Grizmo-head 2.png` at 100px tall (plan override of wireframe's 160px), centered, with the wireframe's 4.5s idle sway (`grizmo-empty-state-wave`, ~2° rotation, bottom-center origin).
- **2.2 Greeting** — Brand-tinted title "Hey, I'm Grizmo!" (Texto `category="label"`) + muted body "Your pricing co-pilot. I'll **explain prices**, model **what-ifs**, compare **peers**, and decode **flags**." (Texto `category="p2"`). Copy verbatim from wireframe's `.fti-title` + `.fti-body`. Bold accents via `<b>` carry darker ink + semibold weight.
- **2.3 Chips** — 4 aggregate chips (`agg-flagged`, `agg-top`, `agg-move`, `agg-unpub`), each `{ id, label, promptId }`, rendered as `GraviButton size="small" shape="round"` with `buttonText` (NOT children — see gotcha). Wrap-aware via `flex-wrap`. Click stub: `console.log("chip clicked", chip.id)` until wiring lands in 3.8.
- **fakeLlm.ts updated** — Renamed `why-23-flagged` → `agg-flagged` (faithful to wireframe id), added 3 new scenarios (`agg-top`, `agg-move`, `agg-unpub`) lifted from wireframe `buildAgg()`. `summarize-30-days`, `compare-three-quotes`, and `_fallback` retained for later phases.
- Verified: TS clean (zero new Grizmo errors), dev :5181 HTTP 200, panel screenshot confirms portrait + greeting + all 4 chips wrap into 2 rows. Click logs fire (`chip clicked agg-top` confirmed via Playwright).

**Phase 1 complete.** Grizmo is reachable in the running app on :5181:
- `<GrizmoProvider>` wraps the `QuoteBookPage` JSX; `grizmo.css` (and tokens) auto-load via the module barrel's side-effect import.
- **1.1 Trigger** — `GrizmoTrigger` in the EOD toolbar (`QuoteBookActionButtons.tsx`, between analytics switch and Last Save). Rounded square 28×28 per wireframe (NOT a circle), `id="grizmo-trigger"`, click → `actions.toggleOpen()`.
- **1.2 Intro bubble** — `GrizmoIntroBubble` portal, anchored to trigger via `getBoundingClientRect()`, copy verbatim from wireframe ("Hi, I'm Grizmo 👋 / Your new pricing co-pilot. Click the G to start, or hit ⌘G."), renders null when `bubbleDismissed || open`. Right-edge clamp prevents off-screen bleed.
- **1.3 Panel** — `GrizmoPanel` portaled to `document.body`, right-docked (top 50, right 16, 400×540), unmounts on close, `slidein 200ms ease-out` (translateY -6px → 0, opacity 0 → 1), `aria-modal="false"` (docked, not blocking), close button focuses on open via rAF.
- **1.4 Shortcut** — `GrizmoShortcuts` renderless component. ⌘G / Ctrl+G global, OPEN-ONLY (no toggle-close on second press, per plan). Single-effect + ref pattern. Skips when typing in `<input>`/`<textarea>`/contentEditable outside `.grizmo-scope` / `#grizmo-trigger`. `preventDefault` to suppress browser's find-next.

## Active task

**Idle — plan complete.** Phases 0 → 7 from `plan.html` are all shipped, plus three follow-ups Frank requested on top (mini-bubble, two-row header, animated conic-gradient shimmer border). The implementation plan has no Phase 8 — Phase 7 was the final phase. Carried-over polish backlog still open (none blocking, none ordered):
1. Tour popover auto-opens on every panel open regardless of `state.view` and overlaps Library/Activity — gate it to `view === 'chat'`.
2. Route `contexts` through `services/fakeLlm.ts` so chip changes affect answer copy.
3. Activity "Save as path" handler seeds a single-step `agg`-scope path — consider letting users pick scope at modal-open time.
4. Pre-existing `forwardRef` console warning (not introduced by us).
5. Close button tooltip shows generic "End session" — wireframe shows a richer tooltip with the discard count.
6. Run the Phase 7 verify suite on touch / keyboard-only flows (we exercised pointer paths only).

When picking up next session: `git status` in `prototype/` to see uncommitted work, run dev :5181 (`yarn start`), open `/PricingEngine/QuoteBookEOD`. Nothing is half-built — every Phase 0-7 piece has a green verify run.

## Recent decisions

- **2026-05-23 (Phase 7)** — Built sequentially on the main thread, single session. Order: extended `types.ts` (`closeConfirmOpen: boolean`) → extended reducer in `GrizmoContext.tsx` (4 new actions: `openCloseConfirm`, `closeCloseConfirm`, `discardSession`, `clearBackups`) → wrote `GrizmoToastHost.tsx/.css` (7.4 first so subsequent pieces can fire toasts) → wrote `GrizmoCloseConfirm.tsx/.css` (7.2) → rewired `GrizmoPanel.tsx` header from single-close to four-control cluster (7.1 + 7.3 in one pass — they share the same flex strip) → updated `GrizmoPanel.css` with `.grizmo-panel__controls` + `.grizmo-panel__ctl` baseline + variants → updated `useGrizmoAsk` to dispatch `clearBackups` at start of every `ask()` (7.5) → updated barrel exports → mounted `<GrizmoToastHost />` in `page.tsx`. ~450 lines new, ~80 modified. No agent swarm — too much shared state (4 actions ripple through all the new components) and the spec is small enough that sequential single-thread beats coordination overhead.
- **2026-05-23 (Phase 7.4 — single-toast policy)** — Initial reducer kept `addToast` as append-to-array. Verify run caught the bug: when toast A's 2500ms timer fired, `dismissToast(A.id)` removed A, leaving toast B at index 0 — and the host re-rendered with B as `latest`, starting a fresh timer for B. So "Restored" never auto-dismissed because the prior "Cleared" toast resurfaced under it. Fix: `addToast` REPLACES `toasts` with a single-element array — matches the wireframe's `showToast` (one element, replace text). One-line reducer change; tests passed on retry. *Lesson:* when the wireframe uses an `id="toast"` singleton DOM node, the state model should mirror that singleton — not a stack — to avoid surprise resurrection.
- **2026-05-23 (Phase 7 — `discardSession` as compound action)** — Plan describes discard as "stash → wipe → setOpen(false)", which would naturally be 3 reducer dispatches (3 re-renders). Instead, wrote `discardSession` as a single compound reducer action that does all three atomically + closes the confirm modal. *Why:* (a) the user sees a single transition (modal vanishes + panel slides out at the same beat), (b) avoids the intermediate state where messages are wiped but modal is still visible, (c) one place to maintain the stash logic. Clear remains its own action (`clearConversation`) because it doesn't close the panel.
- **2026-05-23 (Phase 7.2 — `wrapClassName` not needed, portal to body)** — Phase 6's path modal needed `wrapClassName='grizmo-scope grizmo-path-modal-wrap'` because antd Modal portals outside the panel. Phase 7.2's close-confirm is a custom modal (not antd Modal), so I portal it to `document.body` with `.grizmo-scope` on the backdrop directly — simpler, no antd specificity dance, and the danger-red destructive button (which has heavier styling than antd's primary button) gets clean control via the `__discard` class. *Pattern to remember:* for confirm-style modals with custom buttons, prefer raw portal + tokens over `antd.Modal` — less specificity overhead.
- **2026-05-23 (Phase 7.5 — backup wipe inside `ask()`, not in a separate effect)** — Considered: a `useEffect` on `state.messages` that wipes backups when the list grows beyond 0 (would also cover programmatic message pushes). Rejected: the rule should be "user took a new action", not "messages array grew" — the latter would wipe backups when Undo restores messages from backup, which is the opposite of intent. Putting the `clearBackups` dispatch at the START of `ask()` ties it to the exact user gesture (chip click / composer Enter / path step) and avoids the false positive. *Lesson:* "user took action X" is a verb on the call site, not a derived state observation; the user's gesture is the truth, not the resulting state shape.

- **2026-05-22 (Phase 6)** — Built sequentially on the main thread, no agent swarm. Order: types → reducer extensions (8 new actions) → `services/fakeLlm.ts` mode-stamping (six scenarios + helper) → `state/detectPathCandidate.ts` (pure selector, separate file so it stays testable) → `state/useGrizmoAsk.ts` extended to stamp `promptId/question/mode/rowId` on the grizmo message + run detector after answer → `state/useGrizmoRunPath.ts` (runner hook) → `GrizmoPathCandidate.tsx/.css` (6.1) → `GrizmoMessageList.tsx` rewired to host candidate per-bubble → `GrizmoPathModal.tsx/.css` (6.2 + 6.3 in one component) → `GrizmoPanel.tsx` mounts modal inside the portaled aside → `GrizmoLibrary.tsx` Run + Open wired (Run shows "Running…" + `disabled` while `state.isPathRunning`) → `GrizmoActivity.tsx` Re-run + Save-as-path wired. Sequential single-thread was faster than coordinating subagents because all four UI pieces consume the same reducer extensions and use the same primitives. ~600 lines new, ~150 modified.
- **2026-05-22 (Phase 6.5 — detector placement)** — Detector runs in `useGrizmoAsk` after `updateMessage` resolves. *Catch:* `state.messages` is stale right after dispatch (the closure captures the pre-dispatch snapshot), so the detector would see N-1 messages. *Fix:* synthesize the post-update message list locally by appending the freshly-pushed user + grizmo messages with the partial we just applied, then pass that array to `detectPathCandidate`. Pure-function detector pays off here — easy to drive with a synthetic array. *Pattern to remember:* any post-dispatch derived computation either lives in `useEffect` (rerenders after state settles) OR synthesizes the next state locally.
- **2026-05-22 (Phase 6.2 — antd Modal `visible` vs `open`)** — antd 4 Modal uses `visible` / `onCancel`, not `open` / `onOpenChange`. Same gotcha already logged for Popover (Phase 4.3); rediscovered when TS flagged the prop on first compile. Both Modal and Popover stay on legacy props until the antd 5 upgrade.
- **2026-05-22 (Phase 6.2 — wrap class for tokens)** — antd Modal portals outside `.grizmo-scope`, so Grizmo tokens (`--grizmo-brand`, `--grizmo-surface-2`, etc.) don't reach the body. Fix: `wrapClassName='grizmo-scope grizmo-path-modal-wrap'` lands the class on the antd wrap div, and all CSS selectors prefix `.grizmo-path-modal-wrap` so the tokens cascade. *Why not just `.grizmo-scope`:* the wrap class is appended to antd's existing classes, so we'd be styling against a moving target; a dedicated `__wrap` namespace is cleaner.
- **2026-05-22 (Phase 6.3 — inline editor, not a second modal)** — Plan said "do not create a second modal — slide or expand inside the existing one." Implemented as a single boolean (`pathModal.editorOpen`) that toggles each step row between read-only label and `Input` + reorder/drop action buttons. *Why:* matches the wireframe's `path-editor` pattern and avoids antd Modal-inside-Modal mask z-index dance.
- **2026-05-22 (Phase 6.3 — reorder via up/down buttons, not drag)** — Wireframe uses a drag handle; we ship up/down arrow buttons instead. *Why:* no drag-and-drop library in `package.json`, and pulling one in for a prototype where the user has ≤ ~5 steps is overkill. Keyboard accessibility comes free. If user feedback says drag is missed, revisit in Phase 7 polish.
- **2026-05-22 (Phase 6.4 — runner hook, not a method on the store)** — `useGrizmoRunPath` is a separate hook that composes `useGrizmoAsk`. *Why:* keeps `GrizmoContext` pure (no async side effects in reducers), and the hook can read `state.isPathRunning` + `state.contexts` reactively. Pattern mirrors `useGrizmoAsk` from Phase 3.8.
- **2026-05-22 (Phase 6.4 — `setView('chat')` before running)** — Plan said the runner switches to chat. Implementation: `actions.setView('chat')` then `setPathRunning(true)`. The view switch lands before the first `ask()` so the conversation surface is mounted as messages stream in. The composer footer (which shows only in chat view) ensures the user can interject; runner doesn't disable composer (intentional — user can ask follow-up midway).
- **2026-05-22 (Phase 6.5 — `_fallback` promptId for free-text)** — Composer free-text submits land in the fake LLM's `_fallback` scenario. We stamp `effectivePromptId = promptId ?? '_fallback'` on the message so saved paths can replay free-text steps later. *Why:* the runner needs *something* to pass to `ask({ promptId })`; using `_fallback` keeps the replay deterministic. Side effect: two consecutive composer submits dedupe (same promptId), which is the intended behavior per wireframe's "Deduplicate consecutive identical promptIds" rule.
- **2026-05-22 (Phase 6 — row-scope guard surfaces a toast, not an alert)** — Plan suggested an `alert()` placeholder until Phase 7.4 toasts land. We already have `state.toasts` from Phase 3 plumbing, so the row-scope bail uses `addToast({ kind: 'warn', text: 'Pick a row first to run this path.' })`. *Why:* `alert()` blocks the test runner and feels worse than no-feedback in a prototype. The toast renderer itself is still Phase 7.4 territory — today the toast lives in state without a visual.

- **2026-05-22 (Phase 5)** — Built Phase 5 as a hybrid: I did the shared / structural pieces sequentially (types + reducer extensions, `GrizmoViewNav`, `GrizmoViewSearch`, `GrizmoBookmarkIcon`, `GrizmoPanel` rewire), then fanned out 5.2 + 5.3 to **two parallel agents** (Library and Activity are disjoint files, both consuming the same primitives — perfect parallel surface). Each agent got an `agent-brief`-disciplined prompt (~200-word output cap, explicit "files you may edit / may NOT edit", primitives + state shape spelled out, structured output template). Both returned clean: TS clean, dev :5181 still 200, no edits to shared files. Then I drove end-to-end Playwright verification myself.
- **2026-05-22 (Phase 5.1)** — Replaced the panel header's "Grizmo" `Texto` title with the 3-tab `GrizmoViewNav`. The header now uses `align-items: stretch` and zero-bottom padding so the active-tab underline lines up with the panel's border-bottom, like the wireframe `.tab-strip`. Close button got `flex-shrink: 0` + center self-alignment to survive the new flex regime.
- **2026-05-22 (Phase 5.2)** — Plan diverges from wireframe on library structure: wireframe has Bookmarks|Paths inner tabs, plan mandates Paths|Prompts (Bookmarks become a filter toggle in 5.5). Took the plan. `SavedPrompt` already existed in `types.ts` from Phase 0; only had to add `description: string` for card display and `bookmarked: boolean` to `SavedPath`. Two paths + two prompts seeded with realistic copy. Inner-tab count pills (e.g., "Paths 2") were added by the agent — kept them; not strictly in spec but they reinforce the active state at no cost.
- **2026-05-22 (Phase 5.3)** — Activity entries are derivative in the wireframe (computed from messages on the fly) but the plan locks them as their own `state.activity` array. Kept the plan's model — simpler, decoupled from the chat conversation, and matches `state.activity` already in `types.ts`. Seeded 3 entries with staggered millisecond timestamps so the "3 min / 22 min / 1 h ago" sample matches what a user would see after a day's work.
- **2026-05-22 (Phase 5.4)** — Search input owns local state for snappy typing, debounces 150ms before flushing to the store. A `useEffect` re-syncs local from upstream when the store value changes externally (e.g., user navigates away + back and the DOM input needs to reflect persisted query). × button bypasses debounce. *Why local + debounce instead of controlled-input throttling:* keeps typing instant, prevents a re-render storm in the parent list on every keystroke, and survives the view-switch test (DOM re-mounts).
- **2026-05-22 (Phase 5.5)** — antd has no first-class "bookmark" icon — `BookOutlined` is a book, `StarOutlined` is a star, and the plan explicitly says "bookmark icon, not a star". Shipped a 14px inline-SVG ribbon (`GrizmoBookmarkIcon.tsx`) with `currentColor` so it inherits state color from its parent button. Filled vs outlined via a `filled` boolean. Reused across cards, activity rows, and the bookmark-only filter toggle in the search bar.
- **2026-05-22 (Phase 5.6)** — Per-view search + bookmark-only state lives under `state.search.{library,activity}` and `state.bookmarkedOnly.{library,activity}`. Switching `state.view` doesn't touch any of these (reducer never overwrites them on `setView`). The wireframe used flat keys (`librarySearch`, `activitySearch`); namespaced objects keep adds cheap when a future view needs the same shape. Verified by Playwright: type "triage" in library → switch to activity → type "Pilot" → switch back to library → DOM input still says "triage" + store still has `search.library === 'triage'`.
- **2026-05-22 (Phase 5)** — Per-view `padding` discrepancy: the panel body had `padding: 14px 16px 8px` + `gap: 10px` baked in for the chat view. Adding library/activity views meant their search bars wouldn't flush to panel edges. Two options: strip `--{view}` modifier classes on the body and have each view override locally, OR rewrite the panel body to ship no padding and have each view own its own spacing. Took option 1 (Activity agent added `.grizmo-panel__body--activity { padding: 0; gap: 0; }`; Library has its own internal layout). Less invasive; chat view + has-messages state still work.

- **2026-05-22 (Phase 4)** — Built Phase 4 in-place, sequential single-thread. Step order: types + reducer (added `addContextMode`, `tourDismissed`, `ContextChip.explicit`, plus 6 new actions) → tokens + `grizmo-grid.css` (cell states + column-group tints + add-mode cursor) → `GrizmoActiveBodyClass` body-class toggler → `grid/grizmoScope.ts` (SCOPE_FIELDS map, IDENTITY_FIELDS set, `getCellState` selector) → `grid/withGrizmoCellWiring.ts` (column-defs walker) → `grid/useGrizmoGridWiring` hook → wired into `EndOfDay.tsx` → `GrizmoContextChips` (4.4) → Escape handler in `GrizmoShortcuts` (4.5) → `GrizmoColumnLegend` + `GrizmoTourTrigger` (4.2/4.3) mounted in `QuoteBookActionButtons`. Two course-corrections mid-stream: (a) initial selector treated row chips + scope chips identically, causing scope chips to light entire rows; refactored selector to track `rowChip` vs `cellOrSelectionChip` separately. (b) Composed `onCellClicked` initially still fired the host app's handler, which opened the Quote History drawer on every cell click; added `openRef` so when Grizmo is open, the host handler is bypassed.
- **2026-05-22 (Phase 4)** — **AG Grid won't re-evaluate `cellClass` callbacks just because external state changes.** My first cut had the callback read contexts from a live ref, but cell highlights never appeared because AG Grid only re-paints cells on row data change. Fix: `useGrizmoGridWiring(colDefs, gridApiRef)` accepts the grid ref and calls `gridApi.refreshCells({ force: true })` in a useEffect on `[contexts, open]`. *How to apply:* any future AG Grid feature that drives styling from React state outside the row data needs an explicit refresh; live refs alone are not enough.
- **2026-05-22 (Phase 4)** — Cell tokens live at `:root`, not `.grizmo-scope`. *Why:* AG Grid cells render outside any Grizmo container, so scope-class-prefixed tokens are invisible to them. The risk of leaking into other modules is mitigated by the `--grizmo-*` name prefix and the fact that the cell rules themselves gate on `.grizmo-active` body class.
- **2026-05-22 (Phase 4)** — Click intercept (when `state.open`) skips host app's `onCellClicked`. *Why:* The QuoteBook cell handlers open drawers (Quote History, Valuation, etc.). Letting those fire alongside Grizmo's chip-scope handler made every cell click open a modal AND seed a chip — bad UX. Pattern: when an overlay feature competes for cell clicks, gate the existing handler on the overlay's open state.
- **2026-05-22 (Phase 4)** — antd 4.20 uses `visible` / `onVisibleChange` for Popover, not `open` / `onOpenChange`. Caught by TS. *How to apply:* the rest of this codebase uses Popover/Modal extensively — stick with the legacy prop names until the antd 5 upgrade.
- **2026-05-22 (Phase 4)** — Empty state no longer hides when contexts exist. Old guard was `!hasMessages && state.contexts.length === 0`; now just `!hasMessages`. *Why:* The chip bar is its own strip above the body; the empty-state portrait + greeting + suggestion chips should remain available until the user actually asks something, regardless of chip state.

- **2026-05-22 (Phase 3)** — Built Phase 3 in-place (no agent swarm). 3.1/3.2/3.3 are tightly interleaved (list renders bubbles, bubbles share layout primitives); 3.5/3.6/3.7 are slot components inside the bubble built in 3.3. Sequential single-thread was faster than coordinating subagents on overlapping files.
- **2026-05-22 (Phase 3.5)** — Step status computation (`pending`/`active`/`done`) lives in `GrizmoBubble.computeDisplaySteps()`, NOT in the thinking-steps component. *Why:* Plan rule "no timing logic in the UI, only reflects the data it is given." The component just renders `{label,status}`; status is derived from the message's `thinking[]` + `thinkingComplete` flag.
- **2026-05-22 (Phase 3.8)** — Added a dedicated `appendThinking(id, step)` reducer action instead of reading current thinking via the dev hook + dispatching `updateMessage` with the spread. *Why:* I initially built a `readLatestThinking` helper that reached into `window.__grizmo__.getState()` to avoid stale closure state, but that helper is dev-only — in production builds it would silently overwrite previous steps. A purpose-built reducer action keeps the store pure AND works in prod. Pattern to remember: when a streaming append needs the "current" value, write a reducer action that does the append atomically; don't bridge through external state reads.
- **2026-05-22 (Phase 3.8)** — `useGrizmoAsk` is the single composer/chip/programmatic entry. *Why:* Round-4 lesson — when streaming logic was duplicated per call site, undo + discard got flaky. One hook = one place to fix bugs in the conversation lifecycle.
- **2026-05-22 (Phase 3)** — Switched from wireframe's single-line `<input>` to antd `Input.TextArea` with `autoSize={{ minRows: 1, maxRows: 5 }}` for the composer. *Why:* Plan 3.4 explicitly upgrades to a textarea so multi-line prompts (Enter submits, Shift+Enter newlines) work. The wireframe used input because its earlier prototypes didn't have free-text support.
- **2026-05-22 (Phase 3)** — Composer send button needs the same 4-class specificity bump (`.grizmo-scope .ant-btn.gravi-button.grizmo-composer__send`) as the trigger — Excalibrr's `.gravi-button.filled.default { background: var(--gray-200) }` ties our 3-class selector on specificity and wins on cascade order. *How to apply:* Any future Grizmo button that sets a background through tokens needs the 4-class form.
- **2026-05-22 (Phase 3)** — `AnswerBody.body` widened to `string | AnswerBlock[]` (paragraph / list / table). All current canned scenarios still ship as strings — structured form is opt-in for future compares. *Why:* Plan 3.6 mandates the renderer support both shapes; doing both at one time avoids a later breaking type change.
- **2026-05-22 (Phase 3)** — Source pills are `<button>` not `<a>`. *Why:* Plan 3.7 says deep-link wiring lands in a later phase; today the click is a `console.log` no-op. Using `<button>` keeps focus + Enter activation correct and prevents accidental `href="#"` reloads.
- **2026-05-22 (Phase 3)** — Panel body uses `.has-messages` class to strip its padding when the message list is mounted (list owns its own padding + scroll). *Why:* Avoids double padding + double overflow containers; one place owns scroll, matches Round-4 lesson on scroll flicker.

- **2026-05-22 (Phase 1 cleanup)** — **Excalibrr `.gravi-button.filled.default` defines `background: var(--gray-200)`** and ties any `.grizmo-scope .ant-btn.X` rule on specificity (0,3,0). My token-driven background loses on cascade order. Pattern: bump to (0,4,0) by chaining the antd marker class on the same element — `.grizmo-scope .ant-btn.gravi-button.grizmo-trigger-btn`. Apply to any future Grizmo button that sets a background through tokens.
- **2026-05-22 (Phase 1 cleanup)** — **Intro bubble anchor uses 60-frame rAF poll + ResizeObserver hybrid.** *Why:* trigger lives inside GraviGrid's controlBarProps; grid renders empty first and reflows when data arrives. ResizeObserver only fires when the observed element's box changes — the 28×28 trigger never resizes, just moves. Solution: re-measure every animation frame for ~1s after mount, then attach ResizeObserver for steady-state (window resize, sidebar toggle, font load). *How to apply:* any future Grizmo overlay anchored to a moving target should use the same pattern (see `GrizmoIntroBubble.tsx` for reference).
- **2026-05-22 (Phase 2)** — **GraviButton renders `buttonText`, NOT children.** Caught when chips rendered as empty pills. The library explicitly overrides `children: buttonText` in the underlying antd `Button`. Trigger only ever got away with `<GraviButton>G</GraviButton>` because the "G" was being drawn elsewhere (probably CSS). Pattern going forward: `<GraviButton buttonText={label} ... />`, self-closing. Logged to global memory at `gotcha_gravibutton_buttontext.md` for future projects.
- **2026-05-22 (Phase 2)** — Renamed `why-23-flagged` scenario → `agg-flagged` so chip promptIds match wireframe `data-q` ids 1:1. Added 3 new scenarios (`agg-top`, `agg-move`, `agg-unpub`) lifted from wireframe `buildAgg()`. *Why:* Phase 2.3 plan said promptIds must match scenarios seeded in 0.4 — they didn't; this aligns them with minimal churn.
- **2026-05-22 (Phase 2)** — Greeting is BOTH title + body (wireframe has both `.fti-title` and `.fti-body`). Plan said "single line" but also "copy text exactly as written in wireframe — wireframe wins" — kept both. Title in brand purple, body in muted text.
- **2026-05-22 (Phase 2)** — Portrait is 100px tall (plan spec) instead of wireframe's 160px. Plan explicitly overrode wireframe here. Kept the 4.5s idle sway animation from wireframe — it's the cheapest way to make Grizmo feel alive.
- **2026-05-22 (Phase 2)** — Phase 2 done in-place (no agent swarm). All three steps (2.1/2.2/2.3) edit the same `GrizmoEmptyState.tsx` so parallelizing would have meant merge conflicts. ~150 lines across 5 files done by main thread; faster than coordinating agents.
- **2026-05-22** — **Design-system policy locked:** wireframe is design DIRECTION (look, motion, layout, copy); primitives come from the design system (Excalibrr `GraviButton` / `Texto` / `Horizontal` / `Vertical`, antd `Tooltip` / `Popover` / `Modal` / `Drawer`, `@ant-design/icons` for icons). No more raw `<button>` / typography from scratch. *Why:* Frank caught me letting agents build off the wireframe directly — would have cost focus rings, a11y, theme propagation, and future design-system updates. Applied retroactively to all Phase 1 components (`GrizmoTrigger`, `GrizmoPanel`, `GrizmoIntroBubble`); every Phase ≥ 2 agent gets this constraint explicitly in their prompt.
- **2026-05-22** — CSS selectors for design-system-wrapped elements use `.grizmo-scope .ant-btn.grizmo-XXX` (3-class) instead of `.grizmo-scope .grizmo-XXX` (2-class) so token overrides decisively beat antd's baseline rules regardless of CSS import order. Plus explicit `min-width` to override antd's small-button minimum. *Why:* prevent the prototype from drifting into "raw HTML wins, design system loses" if antd updates its specificity later.
- **2026-05-22** — Outer panel + bubble containers stay custom (`<aside>` + portal for the panel, `<div>` + portal for the bubble). *Why:* antd has no "fixed-height docked side panel" primitive (Drawer is full-height) and antd Popover binds to a child, which doesn't fit our portaled-bubble + remote-trigger model. The CONTENTS use the design system.
- **2026-05-22** — Phase 1 ran as a 2-wave swarm: Wave 1 = one agent (pre-step + trigger, since they share `QuoteBookActionButtons.tsx` and `page.tsx`); Wave 2 = 3 parallel agents (bubble, panel, shortcut) on disjoint files; I did the final barrel + page integration. *Why:* avoids merge conflicts on shared files while still parallelizing the bulk of the UI work.
- **2026-05-22** — `GrizmoIntroBubble.tsx` named separately from `GrizmoBubble.tsx`. *Why:* The plan's "bubble" in Phase 1.2 is a tour-tip popover; in Phase 3.2/3.3 it's a chat message. Two concepts, two components — no name overload.
- **2026-05-22** — Panel uses `aria-modal="false"`, not a blocking modal. *Why:* It's a docked side panel — the QuoteBook behind it stays interactive. Phase 7.2 (close confirmation) is the right time to revisit if the panel ever needs to block.
- **2026-05-22** — ⌘G is open-only by design (not a toggle). *Why:* The wireframe handler is a literal `trigger.click()` which would toggle, but the plan explicitly overrides this — Cmd+G again focuses the composer (Phase 3.4), never closes. Closing belongs to X / Esc.
- **2026-05-22** — Phase 0 finished via 3-agent parallel swarm (steps 0.3, 0.4, 0.5). Coordination point: `types.ts` written first to lock shared shapes. *Why:* fan-out without type drift; one cache miss instead of three sequential.
- **2026-05-22** — Tokens scoped under `.grizmo-scope` with `--grizmo-*` prefix. *Why:* Excalibrr already owns the global `--theme-*` namespace; module-scoped tokens prevent collision and let consumers opt in by adding the class to a Grizmo root.
- **2026-05-22** — Module lives at `prototype/src/modules/Grizmo/` (PascalCase, matching siblings: `PricingEngine`, `ContractManagement`, `Admin`). *Why:* User-facing feature with its own surface — `modules/` is the right home; `components/` is for shared low-level widgets.
- **2026-05-22** — Single public entry: `prototype/src/modules/Grizmo/index.ts` re-exports the full public surface (9 components + `GrizmoProvider` + `useGrizmo` + `askGrizmo` + types). *Why:* Keeps consumers from reaching into internals; one-line edit when wiring up.
- **2026-05-22** — Fake-LLM lives at `services/fakeLlm.ts` with `askGrizmo(prompt, opts?)`. *Why:* Stable signature mirrors a real client so swapping to a live LLM later is a single-file change. Faked responses kept deliberately obvious (no almost-real outputs).
- **2026-05-22** — All component stubs return `null` (not "Coming soon" placeholders). *Why:* Build is provably unaffected; nothing accidentally bleeds into a render path.
- **2026-05-22** — Image filenames preserved as `grizmo-hi 1.png` and `Grizmo-head 2.png` (with spaces). *Why:* User's instruction was to copy them; renaming wasn't requested. Vite handles spaces in imports.
- **2026-05-22** — Recon: insertion point for new toolbar button is `QuoteBookActionButtons.tsx:118–119` (before the "Last Save" display). *Why:* It's the page-scoped toolbar passed to `GraviGrid` as `controlBarProps`; app-shell-level would be wrong scope for a quote-book action.
- **2026-05-22** — Recon: reuse antd `Modal` directly (no wrapper) for the Grizmo modal, modeled on `UploadStatusModal.tsx`. *Why:* Codebase already uses antd `Modal` exclusively; no `useModal` / portal abstraction exists; introducing one for a prototype is overkill.
- **2026-05-22** — Recon: for keyboard shortcuts, use ad-hoc `useEffect` + `keydown` if only one shortcut is needed; build a minimal `useKeyboardShortcut` hook only if multiple. *Why:* No hotkey library in `package.json`; only ~5 keydown listeners across `/src`; following existing pattern keeps the prototype lean.
- **2026-05-22** — Established `project-context.md` workflow at project root. Updated proactively after every meaningful chunk; outside the `prototype/` git worktree (not committed). *Why:* Frank wants to be able to `/clear` and resume across sessions.
- **2026-05-22** — Renamed Freddy project `test-2` → `grizmo`: folder, git branch (`project/test-2` → `project/grizmo`), `.freddy-project.json`, `CLAUDE.md`. *Why:* Project Hub has no rename UI; needed to align name with the Gizmo Option A direction.
- **2026-05-22** — Adopted `Kit Round 4 - Gizmo Option A/gizmo-demo.html` as the final-prototype source of truth. *Why:* Most recent / approved round per Frank.

## Open questions

- **State-shape gaps** (surfaced by the 0.5 agent). Our locked `GrizmoState` does NOT include these wireframe fields. Decision deferred — extend `types.ts` when each phase needs them, not pre-emptively:
  - `isMinimized` (separate from `open` — wireframe has minimize-to-mini-bubble)
  - `position: { x, y } | null` (draggable panel persistence)
  - `activeRowId` / `activeScope` (derive from `contexts[]` via selector OR add explicitly)
  - `addContextMode` boolean (Phase 4.5)
  - Bookmarks, search/filter UI state (`libraryStarOnly`, `librarySearch`, `activitySearch`, `libTab`, `activityRange`, `activityStarOnly`) (Phase 5)
  - `isPathRunning` (Phase 6.4)
  - `firstCloseShown`, `tourDismissed`, `introDismissed`, `savePathDismissedUntilNewTurn`, `dismissedAtMsgId` (Phase 7)
  - `msgIdCounter`, `bmIdCounter` — replace with `crypto.randomUUID()` rather than tracking counters in state
- Phase 1 pre-step: where to mount `<GrizmoProvider>`? Likely wrapping the `QuoteBookPage` (page-scoped) or higher in the app shell (app-scoped). App-scoped lets Grizmo open from anywhere; page-scoped contains the blast radius. Default plan: page-scoped at `QuoteBookPage` unless you say otherwise.

## Gotchas & learnings

- *(none yet)*

## Key references

- **Phased implementation plan (HTML):** `/Users/frankoverland/Documents/Claude/Projects/Ai Opportunities/Implementation Plan/plan.html` — Phase-by-phase prompts (0–7) with verbatim Claude Code instructions per step. **Phase 0 (0.1–0.5) and Phase 1 (1.1–1.4 + pre-step) are complete.** Next: Phase 2 (welcome state — 2.1 portrait, 2.2 greeting, 2.3 chips).
- **Final prototype (visual source of truth):** `/Users/frankoverland/Documents/Claude/Projects/Ai Opportunities/Kit Round 4 - Gizmo Option A/gizmo-demo.html`
- **Lo-fi root:** `/Users/frankoverland/Documents/Claude/Projects/Ai Opportunities/`
- **Working dir:** `/Users/frankoverland/freddy/projects/grizmo/prototype/`
- **Module root:** `/Users/frankoverland/freddy/projects/grizmo/prototype/src/modules/Grizmo/`
- **Recon notes (stack + insertion points):** `/Users/frankoverland/freddy/projects/grizmo/GRIZMO_NOTES.md`
- **UX copy audit (2026-05-26):** `/Users/frankoverland/freddy/projects/grizmo/prototype/grizmo-ux-copy-audit.html` — cross-cutting themes, canonical lexicon, detailed findings. Authoritative for current Grizmo copy.
- **Workflow skills:** `~/.claude/skills/make-implementation-plan/` (claude.ai web) and `~/.claude/skills/bootstrap-plan-project/` (Claude Code). Together they reproduce the design→build handoff that bootstrapped this project. See MEMORY.md entries.
- **Branch:** `project/grizmo` (worktree of `~/freddy/proto-base`), tip at `c34be55` as of 2026-05-26.
- **Dev server:** http://localhost:5181 — `cd prototype && VITE_PORT=5181 yarn start`
- **See also:** `resources/REFERENCES.md` (full AI Opportunities index)

## Session log

- **2026-05-26 (UX copy audit + commit `c34be55`)** — Frank ran a sweeping UX copy + aria-label audit across the Grizmo conversation surface; findings captured in `prototype/grizmo-ux-copy-audit.html` (3 sections: cross-cutting themes, recommended canonical lexicon, detailed findings — 801 lines). Applied the audit to 22 component files (GrizmoActivity, GrizmoAnswerFooter, GrizmoBubble, GrizmoChips, GrizmoCloseConfirm, GrizmoColumnLegend, GrizmoComposer, GrizmoConfidence, GrizmoContextChips, GrizmoDetails, GrizmoEmptyState, GrizmoFeedbackForm, GrizmoFeedbackReceipt, GrizmoIntroBubble, GrizmoLibrary, GrizmoMini, GrizmoPanel, GrizmoPathCandidate, GrizmoPathModal, GrizmoTourTrigger) plus `services/fakeLlm.ts` and `state/{GrizmoContext,detectPathCandidate,useGrizmoAsk}.ts`. Notable copy shifts: welcome bubble now leads with what Grizmo answers ("I answer questions about your pricing rows — costs, margins, deltas. Click **Ask Grizmo** to start, or press **⌘G**.") instead of role-claims; minimize pill label "Grizmo" → "Resume conversation" (aria-label "Reopen Grizmo conversation"). First test scaffold also landed: `src/modules/Grizmo/components/__tests__/selectSuggestions.test.ts` (68 lines). All bundled into a single commit on `project/grizmo`: `c34be55 Grizmo: mini bubble above G + UX copy audit pass` — 28 files, +1204 / −152, including the new audit HTML and test. TS clean, dev :5181 still 200. No push.

- **2026-05-26 (claude.ai + Claude Code skills for the design→build handoff)** — Created two skills under `~/.claude/skills/` to formalize the workflow that produced this project. `/make-implementation-plan` (intended for claude.ai web) generates a polished implementation-plan HTML matching this project's `plan.html` archetype — sticky sidebar nav, phase cards, per-step prompts with Copy buttons, outcome bullets, Verify checks. Bundles `assets/plan-template.html` (verbatim CSS + sidebar shell + JS extracted from THIS project's plan.html, with content placeholders) so future plans match the visual that already worked. `/bootstrap-plan-project` (Claude Code) reads a plan HTML and scaffolds the grizmo-shape project folder (CLAUDE.md, project-context.md, PROJECT_NOTES.md, resources/REFERENCES.md, design-system/lofi/, design-system symlink, empty prototype/) — standalone, no Freddy dependency. Both indexed in `~/.claude/projects/-Users-frankoverland-Documents/memory/MEMORY.md` so future sessions discover them. End-to-end bootstrap was test-run against this project's plan.html into `/tmp/test-bootstrap/grizmo-clone/` — folder tree matched spec, zero unreplaced placeholders, symlink resolved.

- **2026-05-26 (UI polish batch — all in `src/modules/Grizmo/`)** — A run of visual refinements on top of Phase 7 + follow-ups, all verified via Playwright screenshots, TS clean throughout:
  - **Drag + resize** — Panel draggable by the title row (`state.position`, pointer-capture, clamped, persists across minimize, resets on discard). Height-only resize via a diagonal grip at the bottom-right corner (`state.panelHeight`, `cursor: ns-resize`, clamp [220px, 80vh]); width never changes.
  - **Auto-fit height** — Panel shrink-wraps to content (was fixed 540 + scroll), grows to a `max-height: min(80vh, calc(100vh-66px))` cap, then body scrolls. `.grizmo-panel-frame` height auto; `.grizmo-panel__body` is `flex: 1 1 auto`. Empty state is ~600px (150px portrait + 2-row header + chips).
  - **Header** — Two rows: `[8-bit Grizmo head img] Grizmo` title + controls (Undo/Clear/Min/Close) on top, view-nav tabs below. Tabs enlarged to 48px tall / 14px font / 16px icons. Header mark is `assets/Grizmo-8bit.png` (NOT the purple "G" circle). Close-button focus ring switched to `:focus-visible` only (no ring on open/mouse).
  - **Shimmer border (tad.ai model)** — Rebuilt: neutral track (`--grizmo-shimmer-track: rgba(0,0,0,0.07)`) at rest that blends into the page, with TWO multi-color glints (`--grizmo-shimmer-1/2/3` = indigo #6366F1 / violet #A78BFA / magenta #D946EF) orbiting at different rates + directions (`::before` 4.2s CW, `::after` 7s CCW) via two `@property` angles. Crisp 2px ring, no blur/glow. Replaced the earlier single-purple-ring approach.
  - **Corners** — Bottom-right corner clipped to 4px (panel) / 6px (frame ring) so it tucks under the resize grip; other three corners stay 12px.
  - **Empty state** — Portrait swapped to full-body `assets/grizmo-hi 1.png` at 150px. Title "Hey, I'm Grizmo!" → 18px, font-weight 900 (heaviest loaded Lato). Body line forced `text-align: center !important` (Texto applies its own left-align that wins on cascade — `!important` is required).
  - **Composer** — Field focus border green→purple (`--grizmo-brand`) + soft purple glow ring `box-shadow: 0 0 0 3px rgba(113,93,170,0.18)`. Ask button black→brand purple (hover brand-hover), disabled stays gray.
  - **Chips ("Try an aggregate question")** — White bg / light-gray border / medium-gray text at rest; light-purple bg + purple text/border on hover + a one-shot diagonal gradient glimmer (950ms). 4-class selector `.grizmo-scope .ant-btn.gravi-button.grizmo-chip` needed to beat Excalibrr's gray-200 default.
  - **Tour trigger** — "How cell colors work" is now a plain underlined text-link (no capsule), 13px, ⓘ icon. Moved to the LEFT of GraviGrid's Bulk Change button with `order: -1` + a reserved (visibility:hidden) slot when the panel is closed, so Bulk Change never shifts. Popover no longer auto-opens (click-only) and its z-index is 900 (under the panel's 1000). The color-swatch legend strip was removed entirely.
  - **Context chips empty copy** — "No context — Grizmo will answer about the whole view." → "No context · click any cell".


- **2026-05-23 (Phase 7 wrap)** — Built Phase 7 end-to-end in one session, sequential single-thread. Order: extended `types.ts` (closeConfirmOpen) → extended reducer + bound actions in `GrizmoContext.tsx` (openCloseConfirm, closeCloseConfirm, discardSession, clearBackups) → wrote `GrizmoToastHost.tsx/.css` (single-toast singleton, 2500ms auto-dismiss, kind-tinted) → wrote `GrizmoCloseConfirm.tsx/.css` (custom portal-to-body modal with warn icon disc + 3 actions + cancel link, Escape/backdrop cancel, focus on Minimize) → rewrote `GrizmoPanel.tsx` header to host four-control cluster (Undo conditional, Clear disabled-when-empty, Minimize, Close) + mounted `<GrizmoCloseConfirm />` inside the panel → updated `GrizmoPanel.css` with `.grizmo-panel__controls` + shared `.grizmo-panel__ctl` baseline + Undo warm-amber pulse → updated `useGrizmoAsk` to dispatch `clearBackups` at start of every `ask()` + added backup state to dep array → updated barrel exports → mounted `<GrizmoToastHost />` in `page.tsx`. Hit one mid-stream bug during verify: Restored toast didn't auto-dismiss because the previous "Cleared" toast resurfaced under it (append-to-array semantics in `addToast`). Fix: changed reducer to single-toast replace. All 11 verify stages green: empty header (Clear disabled, no Undo), seed conversation (Clear enables), Clear (Undo appears + toast), Undo restores (cell highlights re-apply via context restore), Minimize keeps state (re-open preserves messages), Close opens modal (always, even empty), Cancel keeps panel, Discard wipes + closes (Undo recovers on re-open), new ask after Discard wipes backups (Undo disappears), view-switching preserves backups, empty-backup Undo path. Screenshots p7-01..p7-08 in `$CLAUDE_JOB_DIR`. TS clean (zero new Grizmo errors). Pre-existing `forwardRef` warning still present, not introduced by Phase 7.

- **2026-05-22 (Phase 6 wrap)** — Built Phase 6 end-to-end in one session, sequential single-thread. Order: extended `types.ts` (5 new fields on `GrizmoMessage`, `promptId/mode` on `PathStep`, new `PathModalState` + `PathModalStep`, `pathModal/isPathRunning` on top-level state, `mode?` on `GrizmoAnswer`); extended `GrizmoContext.tsx` reducer + action creators (8 new actions); stamped `mode: 'agg' | 'row'` on six fake-LLM scenarios via a new `answer()` overload; wrote `state/detectPathCandidate.ts` (pure selector — aggregate when latest answer has no rowId OR mode 'aggregate' / 'multi-row'; collapses consecutive identical promptIds; respects `usedInPath`); rewrote `useGrizmoAsk` to stamp `promptId/question/mode/rowId` on the grizmo bubble and run the detector against a *synthesized* post-update message list (state is stale right after dispatch — synthesizing locally is the workaround); wrote `state/useGrizmoRunPath.ts` (row-scope toast guard → `setView('chat')` → sequential `await ask()` → reset); wrote `GrizmoPathCandidate.tsx/.css` (6.1, warm-amber inline prompt under latest bubble, "Save as path" + dismiss); rewired `GrizmoMessageList.tsx` to wrap each message in `.grizmo-message-list__item` so the candidate prompt nests under its bubble; wrote `GrizmoPathModal.tsx/.css` (6.2 + 6.3 in one component — antd Modal with `visible` prop, brand-tinted head, name + description fields, scope badge meta, steps list with inline editor toggle, drop/restore + up/down reorder per step, Cancel/Save footer, Save disabled until name + ≥1 kept step); mounted the modal inside `GrizmoPanel` (lives within the portaled aside so antd's z-index just works); wired `GrizmoLibrary.tsx` Run button to `useGrizmoRunPath` (with `disabled + buttonText='Running…'` while `isPathRunning`); wired Open button to `useGrizmoAsk` with the prompt's body as free-text; wired `GrizmoActivity.tsx` Re-run to `useGrizmoAsk` (`setView('chat')` first), Save-as-path to `openPathModal` with a single-step modal seeded from the entry's title (scope `agg`); updated barrel exports to publish the new components + `useGrizmoRunPath` + `detectPathCandidate`. Hit two mid-stream bugs: (1) antd 4's `Modal` uses `visible` not `open` (same gotcha as Popover in Phase 4 — re-discovered when tsc flagged it). (2) Tour popover from Phase 4 intercepted Playwright clicks; added a `actions.dismissTour()` call at the top of the verify script. Verified end-to-end via `verify-phase6.mjs`: TS clean on Grizmo, all 8 verification stages green (candidate flag flips only after 2 answers, scope=agg, modal opens, name validation toggles Save, editor drops + reorders, Save persists to library with usedInPath markers, Run sequentially streams bubbles + flips view to chat, row-scope path without chip surfaces warn toast). Screenshots p6-01..p6-08 in `$CLAUDE_JOB_DIR`. Only console error is pre-existing forwardRef warning.

- **2026-05-22 (Phase 5 wrap)** — Built Phase 5 in two acts. Act 1 (sequential, main thread): extended `types.ts` (added `bookmarked` to SavedPath, `description` to SavedPrompt, `LibraryInnerTab` union, `ViewSearchState` + `ViewBookmarkedOnlyState`, and the three new fields on `GrizmoState`); extended reducer with 6 new actions + bound action creators + 2 paths / 2 prompts / 3 activity seed; wrote `GrizmoViewNav.tsx/.css` (5.1) with antd-icon tabs; wrote shared `GrizmoBookmarkIcon.tsx` (inline SVG ribbon) and `GrizmoViewSearch.tsx/.css` (search + bookmarked-only toggle, 150ms debounced); rewired `GrizmoPanel.tsx` to host the nav in header, hide composer + context chips outside chat view, and route body content by `state.view`; updated barrel exports. Act 2 (parallel agent swarm, two disjoint files): spawned `general-purpose` agent A on `GrizmoLibrary.tsx/.css` (5.2 — Paths|Prompts inner tabs, two empty states, count pills) and agent B on `GrizmoActivity.tsx/.css` (5.3 — reverse-chrono feed, relative-time helper, three icon-only actions per row). Both agents got `agent-brief`-disciplined prompts (capped output, explicit "do not edit" list, primitive contract + state shape spelled out). Both returned TS-clean. Act 3 (verification, main thread): wrote `verify-phase5.mjs`, ran via Playwright — initial pass surfaced a bad test selector (`aria-label*="ookmark" i` matched the bookmark FILTER button in `GrizmoViewSearch` before the card-level button); fixed to use class-specific selectors and re-ran clean. All assertions passing: 3 nav tabs, chat→library→activity routing, inner Paths|Prompts toggle, search filtering + persistence across view switches, bookmark toggle flipping store state, bookmarked-only filter narrowing the list, keyboard Tab/Space/Enter activating tabs, library/activity counts unchanged after view-switch churn. Screenshots: `$CLAUDE_JOB_DIR/p5-01..p5-10.png`. TS clean (zero new Grizmo errors), dev :5181 still 200. Pre-existing `forwardRef` console warning observed — not introduced by Phase 5.

- **2026-05-22 (Phase 4 wrap)** — Built the full grid integration (cell states, column tints, legend, tour, chips bar, +Add, chips↔grid selector) sequentially in one session. New module: `src/modules/Grizmo/grid/` with `grizmoScope.ts`, `withGrizmoCellWiring.ts`, `useGrizmoGridWiring.ts`. New components: `GrizmoActiveBodyClass`, `GrizmoContextChips`, `GrizmoColumnLegend`, `GrizmoTourTrigger`. New styles: `grizmo-grid.css` (cell-state + column-group rules), `GrizmoContextChips.css`, `GrizmoColumnLegend.css`, `GrizmoTourTrigger.css`. Reducer extensions: `addContextMode`, `tourDismissed`, `ContextChip.explicit`; new actions `replaceContexts`, `clearContexts`, `setAddContextMode`, `toggleAddContextMode`, `dismissTour`. Wired into existing code: `QuoteBookActionButtons.tsx` (mounted legend + tour trigger at front of toolbar), `EndOfDay.tsx` (`useGrizmoGridWiring(baseColumnDefs, gridRef)`), `GrizmoPanel.tsx` (chips bar between header and body, empty-state guard simplified), `GrizmoShortcuts.tsx` (Escape exits add-mode), `QuoteBook/page.tsx` (mounted `GrizmoActiveBodyClass`). Two mid-stream bugs caught: scope chips lighting entire rows (selector refactored to distinguish chip kinds), and host-app cell handlers firing during Grizmo scope (added `openRef` gate). Plus a late AG-Grid lesson: live refs alone don't trigger re-paint; added `refreshCells({force:true})` on context/open change. Verified via Playwright: 15 cells go scope-green + 2 go identity-gray on a seeded row chip (row id 1004); explicit cell chip lights ProposedPrice with brand-purple outline badge; legend + tour popover render in toolbar when panel opens and disappear cleanly when closed. TS clean (zero new Grizmo errors), dev :5181 still 200. Screenshots in `$CLAUDE_JOB_DIR`: p4-01..p4-10.
- **2026-05-22 (Phase 3 wrap)** — Built the entire conversation surface in one session, sequential single-thread. Order: (1) widened `GrizmoMessage.body` + added `thinkingComplete` + `AnswerBlock` types in `types.ts`; (2) added `updateMessage` and `appendThinking` reducer actions in `state/GrizmoContext.tsx`; (3) wrote `GrizmoMessageList.tsx/.css` (3.1); (4) wrote `GrizmoBubble.tsx/.css` covering both user (3.2) and grizmo shell (3.3) — bubble composes thinking + body + footer slots; (5) wrote `GrizmoThinkingSteps.tsx/.css` (3.5) with shimmer + auto-collapse; (6) wrote `GrizmoAnswerBody.tsx/.css` (3.6) supporting string OR `{p,ul,table}` blocks; (7) wrote `GrizmoSourcesFooter.tsx/.css` (3.7); (8) wrote `state/useGrizmoAsk.ts` — the single ask() hook; (9) wrote `GrizmoComposer.tsx/.css` (3.4); (10) wired chips through `useGrizmoAsk`, swapped panel body to message list when populated, mounted composer in footer (3.8); (11) updated barrel exports. Hit one mid-stream design issue: initial draft of `useGrizmoAsk` reached into `window.__grizmo__.getState()` to avoid stale closure state when appending thinking steps — works in dev but silently drops steps in prod where the dev hook is gone. Replaced with a purpose-built `appendThinking` reducer action; logged as a new "Recent decisions" entry. Verified end-to-end via Playwright: chip flow (`agg-flagged` → user bubble → thinking spinner → 4 animated steps → final answer with 3 sources + Medium dot → auto-collapse) AND free-text flow ("Quick check…" → fallback scenario → 3 steps → Low confidence answer). Screenshots in `$CLAUDE_JOB_DIR`. TS clean (zero new Grizmo errors), dev :5181 still HTTP 200.
- **2026-05-22 (Lato font alignment)** — Frank asked Grizmo to use Lato like the rest of Excalibrr. Checked host app: Lato is loaded via `@font-face` in `src/components/shared/Theming/core.less` (Regular, Italic, Bold, BlackItalic TTFs in `@assets/fonts/`) and applied globally with `body { font-family: 'Lato', sans-serif }`. My `--grizmo-font-family` token had `-apple-system, "SF Pro Text", "Inter", system-ui, sans-serif` — overriding Lato anywhere `.grizmo-scope` set `font-family: var(--grizmo-font-family)` (panel, intro bubble, trigger). Updated the token to `'Lato', sans-serif`; mono stack kept as-is. Verified via computed-style audit: trigger, panel header, empty-state title/body, chips, intro bubble all now resolve to `Lato, sans-serif`. Screenshots confirm visual parity with the QuoteBook chrome.
- **2026-05-22 (QuoteBook fixture fix)** — Frank flagged "no data loading in the QuoteBook rows." Mock returned 6 rows correctly (verified via wrapped handler logs), grid received them, but `rowData` filtered down to 0. Root cause: the fixture set `SpreadParentMappingId: 0` on every row, and the grid's `rowsByGroup` reducer filters out rows where `isDefinedAndNotNull(SpreadParentMappingId)` — which returns true for `0`. Production code uses null to mean "no parent spread"; the fixture was modeling it as 0 like a sentinel. One-character fix: `SpreadParentMappingId: null`. Verified: 6 rows visible across Dallas Hub (3) + Houston Terminal (3) groupings. Logged to global memory gotchas.md as a production-code observation for any future fixtures.
- **2026-05-22 (Phase 1 visibility cleanup)** — Frank flagged "I don't see anything on the quote book." Visual audit revealed Phase 1 shipped TS-clean but visually broken: (a) `GrizmoTrigger` used `<GraviButton>G</GraviButton>` — same children-vs-buttonText bug as the chips; button rendered empty. Fix: `buttonText='G'`. (b) Trigger background was `rgb(232,232,232)` instead of brand purple — Excalibrr's `.gravi-button.filled.default { background: var(--gray-200) }` tied my `.grizmo-scope .ant-btn.grizmo-trigger-btn` on specificity (0,3,0) and won on cascade order. Fix: bump selectors to (0,4,0) via `.grizmo-scope .ant-btn.gravi-button.grizmo-trigger-btn`. (c) `GrizmoIntroBubble` anchor stale — bubble measured trigger's position at mount but the grid reflows later, leaving the bubble at `top: 1014px` on a 900px viewport. Fix: switch from one-shot + resize-listener to a 60-frame rAF poll after mount (~1s) followed by a ResizeObserver for steady-state. (d) `/PricingEngine/QuoteBook` returns "Page Not Found" — host-app routing config issue (parent menu has no `defaultRedirect` at the URL level), NOT a Grizmo issue. Trigger mounts correctly on both `/QuoteBookEOD` and `/QuoteBookMidday`. All four issues verified via Playwright; final screenshot shows the purple "G" + intro bubble in the EOD toolbar at the expected position.
- **2026-05-22 (Phase 2 wrap)** — Built `GrizmoEmptyState.tsx` + `.css` composing portrait (100px, sway animation) + greeting (Texto title `label` + Texto body `p2` with bold accents) + chips (`GrizmoChips.tsx` + `.css`, 4 aggregate chips via GraviButton with `buttonText`). Wired into `GrizmoPanel` body via `state.messages.length === 0 && state.contexts.length === 0` guard. Extended `services/fakeLlm.ts` SCENARIOS to seed `agg-flagged` / `agg-top` / `agg-move` / `agg-unpub` (renamed `why-23-flagged` → `agg-flagged`, added 3 new with copy from wireframe `buildAgg()`). Hit one bug: chips initially rendered as empty pills — root cause: GraviButton ignores React children, only renders `buttonText`. Fixed and logged the gotcha to global memory + this project's recent decisions. Verified end-to-end via Playwright screenshot + console-log capture; click handler fires `chip clicked agg-top` as expected. TS clean (zero new errors), dev :5181 still 200.
- **2026-05-22 (design-system refactor)** — After Phase 1 landed, Frank flagged that the Grizmo components were raw `<button>` / `<aside>` / `<div>` with no antd / Excalibrr imports. Refactored `GrizmoTrigger`, `GrizmoPanel`, `GrizmoIntroBubble` to use `GraviButton` + `Texto` + `@ant-design/icons` (`CloseOutlined`). CSS bumped to `.grizmo-scope .ant-btn.grizmo-XXX` 3-class selectors + explicit `min-width` overrides so token-driven styling beats antd's baseline. Outer panel/bubble containers stay custom (no antd primitive fits docked-fixed-height panel or portaled-with-remote-trigger popover). TS clean, dev server :5181 still 200. **Policy added to Recent decisions: wireframe = direction, primitives = design system — applies to all future phases.**
- **2026-05-22 (Phase 1 wrap)** — Wave 1: pre-step + 1.1 trigger (single agent on `QuoteBookActionButtons.tsx` + `GrizmoTrigger.tsx/.css` + `page.tsx` provider mount). Wave 2: 3 parallel agents for 1.2 intro bubble (portal, trigger-anchored), 1.3 panel container (portal, right-docked, slidein), 1.4 ⌘G shortcut (renderless, open-only). Integration: added `GrizmoIntroBubble` + `GrizmoShortcuts` to barrel exports and mounted all three inside `<GrizmoProvider>` in `QuoteBookPage`. TS clean, dev server :5181 at 200.
- **2026-05-22 (Phase 0 wrap)** — Locked shared types contract at `src/modules/Grizmo/types.ts`. Ran 3-agent parallel swarm for steps 0.3 (design tokens) + 0.4 (fake LLM) + 0.5 (state store). All 3 reported clean. Verification: `tsc --noEmit` zero errors in Grizmo module, dev server :5181 still 200, no off-target edits. 17 files now under the module.
- **2026-05-22 (scaffold)** — Created `prototype/src/modules/Grizmo/` with 14 stub files + 2 images + README. Verified zero external imports (Grizmo dormant) and dev server still healthy on :5181. No installs.
- **2026-05-22 (recon)** — Ran read-only recon via 3 parallel Explore subagents (tech stack, grid+toolbar, modal+hotkeys). Wrote `GRIZMO_NOTES.md` at repo root. Verified all 9 cited paths exist; `prototype/` worktree untouched. No installs.
- **2026-05-22** — Renamed project test-2 → grizmo (folder, branch, metadata, CLAUDE.md). Created `resources/REFERENCES.md` pointing to AI Opportunities. Restarted dev server on :5181. Established this context-file workflow.
