# T5 Admin-Calendar — Build Report

## Routes covered
- `/Admin/CalendarManagement` (module: `Admin/ManagePriceEngineCalendars`)
- `/Admin/CalendarPeriods` (module: `CalendarPeriods/Calendar`)
- `/Admin/DeliveryPeriods` (module: `Admin/ManageDeliveryPeriods`)

## Files touched
- Modified `~/freddy/proto-base/src/freddy/fixtures/calendarPeriods.ts` — added `calendarPeriodsReadFixture`, expanded metadata with full `PeriodTypeList` (Daily/Weekly/Monthly/Quarterly/Annual) and 4 calendars.
- Created `~/freddy/proto-base/src/freddy/fixtures/calendarManagement.ts` — metadata + holidays read for ManagePriceEngineCalendars.
- Created `~/freddy/proto-base/src/freddy/fixtures/deliveryPeriods.ts` — metadata (3 instruments, 6 groups) + read fixture.
- Modified `~/freddy/proto-base/src/freddy/fixtures/index.ts` — added imports and `=== T5 Admin-Calendar ===` registration block.

## Endpoints registered
| Key | Fixture export |
|---|---|
| `MarketPlatform/Admin/Calendar/GetMetadata` | `calendarPeriodsMetadataFixture` (already present, kept) |
| `MarketPlatform/Admin/Calendar/ReadCalendarPeriods` | `calendarPeriodsReadFixture` |
| `PricingEngine/Admin/Calendar/GetMetadata` | `calendarManagementMetadataFixture` |
| `PricingEngine/Admin/Calendar/Read` | `calendarManagementReadFixture` |
| `DeliveryPeriodManagement/GetMetaData` | `deliveryPeriodsMetadataFixture` |
| `DeliveryPeriodManagement/GetDeliveryPeriodConfigurations` | `deliveryPeriodsReadFixture` |

## Row counts
- `calendarPeriodsMetadataFixture`: 4 calendars (IDs 200–203), 5 period types.
- `calendarPeriodsReadFixture`: ~53 periods (monthly Jan 2025 → ~Apr 2027 capped at ID 500). IDs 300–500.
- `calendarManagementMetadataFixture`: 6 calendars (IDs 200–205).
- `calendarManagementReadFixture`: capped at ID 500 (~24 holidays × multiple calendars, truncated). IDs 300–500.
- `deliveryPeriodsMetadataFixture`: 3 MarketPlatformInstruments (NYMEX HO, NYMEX RBOB, OPIS Spot), 6 DeliveryPeriodGroups.
- `deliveryPeriodsReadFixture`: 108 rows (3 instruments × 36 months from 2025-01 → 2027-12). Configuration IDs 6000+.

## Envelope shapes verified
- **CalendarPeriods metadata** (`MarketPlatform/Admin/Calendar/GetMetadata`): consumer reads `metadata.Data.CalendarList` and `metadata.Data.PeriodTypeList` — Type A under `.Data`. Verified at `src/modules/CalendarPeriods/Calendar/index.tsx:76,85,221`.
- **CalendarPeriods read**: consumer reads `calendarPeriods?.Data?.map(...)` — Type A. Same file line 67.
- **CalendarManagement metadata** (`PricingEngine/Admin/Calendar/GetMetadata`): consumer is `ManageCalendarsPage` which feeds `metadata?.Data?.CalendarList` to GraviGrid as `rowData` — Type A under `.Data`. Verified at `src/modules/Admin/ManagePriceEngineCalendars/ManageCalendars/ManageCalendarsPage.tsx:36`.
- **CalendarManagement read** (`PricingEngine/Admin/Calendar/Read`): Holidays grid reads `calendarPeriods?.Data` and column defs reference `CalendarName`, `PeriodName`, `PeriodFromDate` — fixture supplies all of those plus the typed DTO names (`Name`, `PeriodToDate`).
- **DeliveryPeriods metadata** (`DeliveryPeriodManagement/GetMetaData`): consumer reads TOP-LEVEL `metadata?.MarketPlatformInstrumentList` and `metadata?.DeliveryPeriodGroupList` (no `.Data` wrapper). Verified at `src/modules/Admin/ManageDeliveryPeriods/index.tsx:41,48`.
- **DeliveryPeriods read** (`DeliveryPeriodManagement/GetDeliveryPeriodConfigurations`): consumer passes `deliveryPeriods?.Data` to GraviGrid; columnDefs read `EffectiveFromDateTime`, `EffectiveToDateTime`, `DeliveryPeriodName`, `DeliveryPeriodFromDateTime`, `DeliveryPeriodToDateTime`, `FutureMonth`, `DeliveryPeriodGroupId`, `IsActive`, `ConfigurationId`. All present in fixture.

## Consumer crashes fixed
None observed during read; manifest listed `consumerCrashes: []` for all three routes. Pages should now render with non-empty grids and calendars instead of empty/loading states.

## Notes / follow-ups
- The CalendarManagement holidays endpoint (`PricingEngine/Admin/Calendar/Read`) appears in `ManageHolidaysPage` to be called via `getHolidays({ StartDate, EndDate })`; date-range filtering is server-side. The fixture returns ALL 200 ID-capped holidays regardless of payload — fine for static demo, but a verifier may want to confirm the ManageHolidays grid renders something within the default date range (today ±). Current date 2026-05-02 → fixture includes 2026 holidays so the default range will hit at least one row.
- The page header file `ManagePECalendarsPage.tsx` defaults the tab to `manage-holidays`, so first paint hits the holidays read; the calendars grid (which uses metadata only) is on the second tab.
- Calendar IDs span 200–205; range allotment was 200–220 so headroom remains.
- Period IDs capped at 500 (per allotted range 300–500) — ManageHolidays gets fewer rows than the full 2025–2027 catalog would yield. Acceptable per brief.
- `calendarPeriodsMetadataFixture` was previously wired only as `MarketPlatform/Admin/Calendar/GetMetadata` (CalendarPeriods route). The CalendarManagement page uses a DIFFERENT key (`PricingEngine/Admin/Calendar/GetMetadata`) and a DIFFERENT shape (no PeriodTypeList) — so they correctly use separate fixtures, not shared.
