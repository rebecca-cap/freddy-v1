# T2 Admin-Catalog — Build Report

## Routes covered
- /Admin/Locations (was: crash)
- /Admin/Products (was: crash)
- /Admin/Instruments (was: crash) — see "Open Issues"
- /Admin/MarketPlatformSetups (was: empty)
- /SuperUserAdmin/MPIManagement (was: populated, now richer)

## Files touched
- `src/freddy/fixtures/locations.ts` — rewrote, 12 locations (IDs 5001–5012), full LocationOverviewData shape; metadata now includes `LocationGroups` (5 groups) plus `LocationGroupList` alias and `EditableSources`.
- `src/freddy/fixtures/products.ts` — rewrote, 12 products (IDs 7001–7012), full ProductOverviewData shape; metadata now includes `ProductGroups` (5 groups) plus `ProductGroupList` alias and `EditableSources`.
- `src/freddy/fixtures/manageInstruments.ts` — rewrote. 8 MPIs (IDs 6001–6008), 8 nested subtypes (IDs 6101–6108) covering Prompt/Forward/Contract/TAS/Direct/Allocation/Cycle/Bid trade types. Metadata covers all `MPIMetadata` lists.
- `src/freddy/fixtures/marketPlatformSetups.ts` — NEW. ~28 TradeEntrySetup rows (TradeEntrySetupId 4001+) cross-referencing locations 5001–5012, products 7001–7008, MPIs 6001–6006.
- `src/freddy/fixtures/index.ts` — appended `// === T2 Admin-Catalog ===` banner registering the two new TradeEntrySetup endpoints.

## Endpoints added (key → fixture export)
- `MarketPlatform/Admin/TradeEntrySetup/GetMetaData` → `marketPlatformSetupsMetadataFixture`
- `MarketPlatform/Admin/TradeEntrySetup/Read` → `marketPlatformSetupsReadFixture`

(Existing endpoints already wired by an earlier pass: `Admin/LocationManagement/{GetMetaData,read}`, `Admin/ProductManagement/{GetMetaData,read}`, `marketPlatform/admin/marketPlatformInstrument/{GetAllMarketPlatformInstruments,GetMetaData}`. Their fixtures are now populated rather than empty.)

## Row counts
- locationsReadFixture.Data: 12
- locationsMetadataFixture.Data.LocationGroups: 5
- productsReadFixture.Data: 12
- productsMetadataFixture.Data.ProductGroups: 5
- manageInstrumentsReadFixture.Data: 8 MPIs (with 8 subtypes nested)
- marketPlatformSetupsReadFixture.Data: 28

## Consumer crashes addressed
- `src/modules/Admin/ManageProducts/components/ManagementPane/ManageProductGroups/index.tsx:12` — `metadata?.Data?.ProductGroups.map(...)` now resolves; fixture provides 5 groups with `{Text, Value, GroupingValue}` shape. Note: manifest cited line 33 but the actual `.map` site is line 12 (May 2026). The handleEdit at line 25 also iterates productGroups; both safe now.
- `src/modules/Admin/ManageLocations/components/ManagementPane/ManageLocationGroups/index.tsx:12` — same pattern. Fixed via `LocationGroups` field in `locationsMetadataFixture.Data`.
- `src/modules/Admin/ManageProducts/components/ManagementPane/RelatedProducts/index.tsx:11` — also reads `metadata?.Data?.ProductGroups.map`; same fix covers it.
- /SuperUserAdmin/MPIManagement (`ManageMPIsPage`): grid now has 8 rows; subtypes tab gets 8 flattened subtypes via `getAllSubtypes`. Metadata fields cover all consumer references in `ManageMPIsGrid`/`ManageSubtypesGrid` cell editors.

## Open issues / deferred to verify swarm
- **/Admin/Instruments** is mapped in `pageConfig.tsx:438` to `ManagePriceInstrumentsPage`, which calls `usePriceInstruments().useMetadataQuery()` → endpoint `PriceInstrumentUpload/GetPriceInstrumentUploadData`. The actual crash site is `src/modules/Admin/ManagePriceInstruments/index.tsx:192`: `instrumentMetadata?.Data?.PricePublisherList.map(...)`. The manifest cites `src/modules/Admin/ManagePriceInstruments/index.tsx:263` (file is 256 lines — line stale) and pins the fixture to `manageInstruments.ts`, but the consumer reads from `priceInstrumentUpload.ts` which is **owned by T3**. Per the "stay in your lane" rule I did NOT edit T3's file. Verify swarm should add a `PricePublisherList` array (e.g. `[{Text:'OPIS',Value:'1',GroupingValue:null}, ...]`) to `priceInstrumentUploadFixture.Data` (T3 fixture) to clear this crash.
- The /Admin/Instruments fixtureFile entry in `manifest.json` (`src/freddy/fixtures/manageInstruments.ts`) appears mis-targeted — the real consumer is `priceInstrumentUpload.ts` (T3), and `manageInstruments.ts` is what /SuperUserAdmin/MPIManagement consumes. Suggest updating the manifest to reflect the correct mapping.
- Cross-team contract: my Location IDs are 5001–5012, Product IDs 7001–7012, MPI IDs 6001–6008 (with subtype IDs 6101–6108). T1 (counterparties), T6 (quoteBook), T7 (contracts) should align their MappedProductIds/MappedLocationIds/MarketPlatformInstrumentId references against these.
